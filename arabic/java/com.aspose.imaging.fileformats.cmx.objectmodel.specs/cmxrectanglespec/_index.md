---
title: "CmxRectangleSpec"
second_title: "مرجع API لـ Aspose.Imaging للـ Java"
description: "يمثّل المعلومات الهندسية المحددة لمستطيل."
type: docs
weight: 16
url: /ar/java/com.aspose.imaging.fileformats.cmx.objectmodel.specs/cmxrectanglespec/
---
**Inheritance:**
java.lang.Object

**All Implemented Interfaces:**
[com.aspose.imaging.fileformats.cmx.objectmodel.specs.ICmxObjectSpec](../../com.aspose.imaging.fileformats.cmx.objectmodel.specs/icmxobjectspec)
```
public class CmxRectangleSpec implements ICmxObjectSpec
```

يمثّل المعلومات الهندسية المحددة لمستطيل.
## المنشئات

| المنشئ | الوصف |
| --- | --- |
| [CmxRectangleSpec()](#CmxRectangleSpec--) |  |
## الطرق

| طريقة | الوصف |
| --- | --- |
| [getCenterX()](#getCenterX--) | يحصل على إحداثي X لمركز المستطيل. |
| [setCenterX(float value)](#setCenterX-float-) | يضبط إحداثي X لمركز المستطيل. |
| [getCenterY()](#getCenterY--) | يحصل على إحداثي Y لمركز المستطيل. |
| [setCenterY(float value)](#setCenterY-float-) | يضبط إحداثي Y لمركز المستطيل. |
| [getWidth()](#getWidth--) | يحصل على عرض المستطيل. |
| [setWidth(float value)](#setWidth-float-) | يضبط عرض المستطيل. |
| [getHeight()](#getHeight--) | يحصل على ارتفاع المستطيل. |
| [setHeight(float value)](#setHeight-float-) | يضبط ارتفاع المستطيل. |
| [getRadius()](#getRadius--) | يحصل على نصف قطر زوايا المستطيل المستديرة. |
| [setRadius(float value)](#setRadius-float-) | يضبط نصف قطر زوايا المستطيل المستديرة. |
| [getAngle()](#getAngle--) | يحصل على زاوية دوران المستطيل. |
| [setAngle(float value)](#setAngle-float-) | يضبط زاوية دوران المستطيل. |
| [toString()](#toString--) | يعيد سلسلة تمثل هذه الحالة. |
| [equals(Object o)](#equals-java.lang.Object-) | تحقق مما إذا كانت الكائنات متساوية. |
| [hashCode()](#hashCode--) | احصل على رمز التجزئة للكائن الحالي. |
### CmxRectangleSpec() {#CmxRectangleSpec--}
```
public CmxRectangleSpec()
```


### getCenterX() {#getCenterX--}
```
public final float getCenterX()
```


يحصل على إحداثي X لمركز المستطيل. يُقاس بوحدات المسافة الشائعة في المستند.

**Returns:**
float - إحداثي X لمركز المستطيل.
### setCenterX(float value) {#setCenterX-float-}
```
public final void setCenterX(float value)
```


يضبط إحداثي X لمركز المستطيل. يُقاس بوحدات المسافة الشائعة في المستند.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | float | إحداثي X لمركز المستطيل. |

### getCenterY() {#getCenterY--}
```
public final float getCenterY()
```


يحصل على إحداثي Y لمركز المستطيل. يقاس بوحدات المسافة الشائعة في المستند.

**Returns:**
float - إحداثي Y لمركز المستطيل.
### setCenterY(float value) {#setCenterY-float-}
```
public final void setCenterY(float value)
```


يضبط إحداثي Y لمركز المستطيل. يقاس بوحدات المسافة الشائعة في المستند.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | float | إحداثي Y لمركز المستطيل. |

### getWidth() {#getWidth--}
```
public final float getWidth()
```


يحصل على عرض المستطيل. يقاس بوحدات المسافة الشائعة في المستند.

**Returns:**
float - عرض المستطيل.
### setWidth(float value) {#setWidth-float-}
```
public final void setWidth(float value)
```


يضبط عرض المستطيل. يقاس بوحدات المسافة الشائعة في المستند.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | float | عرض المستطيل. |

### getHeight() {#getHeight--}
```
public final float getHeight()
```


يحصل على ارتفاع المستطيل. يقاس بوحدات المسافة الشائعة في المستند.

**Returns:**
float - ارتفاع المستطيل.
### setHeight(float value) {#setHeight-float-}
```
public final void setHeight(float value)
```


يضبط ارتفاع المستطيل. يقاس بوحدات المسافة الشائعة في المستند.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | float | ارتفاع المستطيل. |

### getRadius() {#getRadius--}
```
public final float getRadius()
```


يحصل على نصف قطر زوايا المستطيل المستديرة. إذا كانت قيمته `0` فإن المستطيل لا يحتوي على زوايا مستديرة. يقاس بوحدات المسافة الشائعة في المستند.

**Returns:**
float - نصف قطر زوايا المستطيل المستديرة.
### setRadius(float value) {#setRadius-float-}
```
public final void setRadius(float value)
```


يضبط نصف قطر زوايا المستطيل المستديرة. إذا كانت قيمته `0` فإن المستطيل لا يحتوي على زوايا مستديرة. يقاس بوحدات المسافة الشائعة في المستند.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | float | نصف قطر زوايا المستطيل المستديرة. |

### getAngle() {#getAngle--}
```
public final float getAngle()
```


يحصل على زاوية دوران المستطيل. يقاس بالراديان.

**Returns:**
float - زاوية دوران المستطيل.
### setAngle(float value) {#setAngle-float-}
```
public final void setAngle(float value)
```


يضبط زاوية دوران المستطيل. يقاس بالراديان.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | float | زاوية دوران المستطيل. |

### toString() {#toString--}
```
public String toString()
```


يعيد سلسلة تمثل هذه الحالة.

**Returns:**
java.lang.String - سلسلة تمثل هذه الحالة.
### equals(Object o) {#equals-java.lang.Object-}
```
public boolean equals(Object o)
```


تحقق مما إذا كانت الكائنات متساوية.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| o | java.lang.Object | الكائن الآخر. |

**Returns:**
boolean - نتيجة مقارنة المساواة.
### hashCode() {#hashCode--}
```
public int hashCode()
```


احصل على رمز التجزئة للكائن الحالي.

**Returns:**
int - رمز التجزئة.
