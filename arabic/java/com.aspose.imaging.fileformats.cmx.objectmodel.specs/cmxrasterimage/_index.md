---
title: "CmxRasterImage"
second_title: "مرجع API لـ Aspose.Imaging للـ Java"
description: "يمثّل البيانات المحددة للصور النقطية."
type: docs
weight: 15
url: /ar/java/com.aspose.imaging.fileformats.cmx.objectmodel.specs/cmxrasterimage/
---
**Inheritance:**
java.lang.Object

**All Implemented Interfaces:**
[com.aspose.imaging.fileformats.cmx.objectmodel.specs.ICmxObjectSpec](../../com.aspose.imaging.fileformats.cmx.objectmodel.specs/icmxobjectspec)
```
public class CmxRasterImage implements ICmxObjectSpec
```

يمثّل البيانات المحددة للصور النقطية.
## المنشئات

| المنشئ | الوصف |
| --- | --- |
| [CmxRasterImage()](#CmxRasterImage--) |  |
## الطرق

| طريقة | الوصف |
| --- | --- |
| [getType()](#getType--) | يحصل على نوع الصورة. |
| [setType(int value)](#setType-int-) | يضبط نوع الصورة. |
| [getCompression()](#getCompression--) | يسترجع الضغط. |
| [setCompression(int value)](#setCompression-int-) | يعيّن الضغط. |
| [getSize()](#getSize--) | يحصل على حجم الصورة. |
| [setSize(long value)](#setSize-long-) | يضبط حجم الصورة. |
| [getCompressedSize()](#getCompressedSize--) | يحصل على الحجم المضغوط للصورة. |
| [setCompressedSize(long value)](#setCompressedSize-long-) | يضبط الحجم المضغوط للصورة. |
| [isMask()](#isMask--) | يحصل على قيمة تشير إلى ما إذا كان هذا الكائن قناعًا. |
| [setMask(boolean value)](#setMask-boolean-) | يضبط قيمة تشير إلى ما إذا كان هذا الكائن قناعًا. |
| [getColorModel()](#getColorModel--) | يحصل على نموذج اللون. |
| [setColorModel(long value)](#setColorModel-long-) | يضبط نموذج اللون. |
| [getWidth()](#getWidth--) | يحصل على عرض الصورة. |
| [setWidth(long value)](#setWidth-long-) | يضبط عرض الصورة. |
| [getHeight()](#getHeight--) | يحصل على ارتفاع الصورة. |
| [setHeight(long value)](#setHeight-long-) | يضبط ارتفاع الصورة. |
| [getBitsPerPixel()](#getBitsPerPixel--) | يحصل على عدد البتات لكل بكسل. |
| [setBitsPerPixel(long value)](#setBitsPerPixel-long-) | يضبط عدد البتات لكل بكسل. |
| [getBytesPerLine()](#getBytesPerLine--) | يحصل على حجم السطر. |
| [setBytesPerLine(long value)](#setBytesPerLine-long-) | يضبط حجم السطر. |
| [getColorPalette()](#getColorPalette--) | يحصل على مصفوفة لوحة الألوان. |
| [setColorPalette(int[] value)](#setColorPalette-int---) | يضبط مصفوفة لوحة الألوان. |
| [getRawData()](#getRawData--) | يحصل على بيانات البايت الخام للصورة. |
| [setRawData(byte[] value)](#setRawData-byte---) | يضبط بيانات البايت الخام للصورة. |
| [toString()](#toString--) | يعيد سلسلة تمثل هذه الحالة. |
| [equals(Object o)](#equals-java.lang.Object-) | تحقق مما إذا كانت الكائنات متساوية. |
| [hashCode()](#hashCode--) | احصل على رمز التجزئة للكائن الحالي. |
### CmxRasterImage() {#CmxRasterImage--}
```
public CmxRasterImage()
```


### getType() {#getType--}
```
public final int getType()
```


يحصل على نوع الصورة.

**Returns:**
int - نوع الصورة.
### setType(int value) {#setType-int-}
```
public final void setType(int value)
```


يضبط نوع الصورة.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | int | نوع الصورة. |

### getCompression() {#getCompression--}
```
public final int getCompression()
```


يسترجع الضغط.

**Returns:**
int - الضغط.
### setCompression(int value) {#setCompression-int-}
```
public final void setCompression(int value)
```


يعيّن الضغط.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | int | الضغط. |

### getSize() {#getSize--}
```
public final long getSize()
```


يحصل على حجم الصورة. يُقاس بالبايت.

**Returns:**
long - حجم الصورة.
### setSize(long value) {#setSize-long-}
```
public final void setSize(long value)
```


يضبط حجم الصورة. يُقاس بالبايت.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | long | حجم الصورة. |

### getCompressedSize() {#getCompressedSize--}
```
public final long getCompressedSize()
```


يحصل على الحجم المضغوط للصورة. يُقاس بالبايت.

**Returns:**
long - الحجم المضغوط للصورة.
### setCompressedSize(long value) {#setCompressedSize-long-}
```
public final void setCompressedSize(long value)
```


يضبط الحجم المضغوط للصورة. يُقاس بالبايت.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | long | الحجم المضغوط للصورة. |

### isMask() {#isMask--}
```
public final boolean isMask()
```


يحصل على قيمة تشير إلى ما إذا كان هذا الكائن قناعًا.

**Returns:**
boolean - قيمة تشير إلى ما إذا كان هذا الكائن قناعًا.
### setMask(boolean value) {#setMask-boolean-}
```
public final void setMask(boolean value)
```


يضبط قيمة تشير إلى ما إذا كان هذا الكائن قناعًا.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | boolean | قيمة تشير إلى ما إذا كان هذا الكائن قناعًا. |

### getColorModel() {#getColorModel--}
```
public final long getColorModel()
```


يحصل على نموذج اللون.

**Returns:**
long - نموذج اللون.
### setColorModel(long value) {#setColorModel-long-}
```
public final void setColorModel(long value)
```


يضبط نموذج اللون.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | long | نموذج اللون. |

### getWidth() {#getWidth--}
```
public final long getWidth()
```


يحصل على عرض الصورة. يُقاس بالبكسل.

**Returns:**
long - عرض الصورة.
### setWidth(long value) {#setWidth-long-}
```
public final void setWidth(long value)
```


يضبط عرض الصورة. يُقاس بالبكسل.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | long | عرض الصورة. |

### getHeight() {#getHeight--}
```
public final long getHeight()
```


يحصل على ارتفاع الصورة. يُقاس بالبكسل.

**Returns:**
long - ارتفاع الصورة.
### setHeight(long value) {#setHeight-long-}
```
public final void setHeight(long value)
```


يضبط ارتفاع الصورة. يُقاس بالبكسل.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | long | ارتفاع الصورة. |

### getBitsPerPixel() {#getBitsPerPixel--}
```
public final long getBitsPerPixel()
```


يحصل على عدد البتات لكل بكسل.

**Returns:**
long - عدد البتات لكل بكسل.
### setBitsPerPixel(long value) {#setBitsPerPixel-long-}
```
public final void setBitsPerPixel(long value)
```


يضبط عدد البتات لكل بكسل.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | long | عدد البتات لكل بكسل. |

### getBytesPerLine() {#getBytesPerLine--}
```
public final long getBytesPerLine()
```


يحصل على حجم السطر. يُقاس بالبايت.

**Returns:**
long - حجم السطر.
### setBytesPerLine(long value) {#setBytesPerLine-long-}
```
public final void setBytesPerLine(long value)
```


يضبط حجم السطر. يُقاس بالبايت.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | long | حجم السطر. |

### getColorPalette() {#getColorPalette--}
```
public final int[] getColorPalette()
```


يحصل على مصفوفة لوحة الألوان. العناصر هي قيم ألوان ARGB ممثلة في `int`

**Returns:**
int[] - مصفوفة لوحة الألوان.
### setColorPalette(int[] value) {#setColorPalette-int---}
```
public final void setColorPalette(int[] value)
```


يضبط مصفوفة لوحة الألوان. العناصر هي قيم ألوان ARGB ممثلة في `int`

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | int[] | مصفوفة لوحة الألوان. |

### getRawData() {#getRawData--}
```
public final byte[] getRawData()
```


يحصل على بيانات البايت الخام للصورة.

**Returns:**
byte[] - البيانات الخام للبايت للصورة.
### setRawData(byte[] value) {#setRawData-byte---}
```
public final void setRawData(byte[] value)
```


يضبط بيانات البايت الخام للصورة.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| القيمة | byte[] | البيانات الخام للبايت للصورة. |

### toString() {#toString--}
```
public String toString()
```


يعيد سلسلة تمثل هذه الحالة.

**Returns:**
java.lang.String - سلسلة تمثل هذه الحالة.
### equals(Object o) {#equals-java.lang.Object-}
```
public boolean equals(Object o)
```


تحقق مما إذا كانت الكائنات متساوية.

**Parameters:**
| معامل | نوع | الوصف |
| --- | --- | --- |
| o | java.lang.Object | الكائن الآخر. |

**Returns:**
boolean - نتيجة مقارنة المساواة.
### hashCode() {#hashCode--}
```
public int hashCode()
```


احصل على رمز التجزئة للكائن الحالي.

**Returns:**
int - رمز التجزئة.
