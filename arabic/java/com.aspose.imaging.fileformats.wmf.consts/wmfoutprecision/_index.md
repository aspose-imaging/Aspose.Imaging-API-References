---
title: "WmfOutPrecision"
second_title: "مرجع API لـ Aspose.Imaging للـ Java"
description: "يحدد تعداد OutPrecision القيم للدقة الناتجة، وهو المتطلب لمطابق الخطوط لتطابق معلمات الخط المحددة بما في ذلك الارتفاع والعرض واتجاه الحرف والانحراف والمسافة ونوع الخط."
type: docs
weight: 27
url: /ar/java/com.aspose.imaging.fileformats.wmf.consts/wmfoutprecision/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class WmfOutPrecision extends System.Enum
```

يحدد تعداد OutPrecision القيم الخاصة بدقة الإخراج، وهي المتطلبات التي يحتاجها مخطط الخطوط لمطابقة معلمات الخط المحددة، بما في ذلك الارتفاع، العرض، اتجاه الحرف، الانحراف، التباعد، ونوع الخط.
## الحقول

| حقل | الوصف |
| --- | --- |
| [Default](#Default) | قيمة تحدد السلوك الافتراضي. |
| [String](#String) | قيمة تُرجع عند تعداد الخطوط المرسومة. |
| [Stroke](#Stroke) | قيمة تُرجع عند تعداد خطوط TrueType وغيرها من الخطوط المتجهة وخطوط المتجه. |
| [Tt](#Tt) | قيمة تحدد اختيار خط TrueType عندما يحتوي النظام على عدة خطوط بنفس الاسم. |
| [Device](#Device) | قيمة تحدد اختيار خط الجهاز عندما يحتوي النظام على عدة خطوط بنفس الاسم. |
| [Raster](#Raster) | قيمة تحدد اختيار خط مرسوم عندما يحتوي النظام على عدة خطوط بنفس الاسم. |
| [TtOnly](#TtOnly) | قيمة تحدد المتطلب لخطوط TrueType فقط. |
| [Outline](#Outline) | قيمة تحدد المتطلب لخطوط TrueType وغيرها من الخطوط المتجهة. |
| [ScreenOutline](#ScreenOutline) | قيمة تحدد تفضيلًا لخطوط TrueType وغيرها من الخطوط المتجهة. |
| [PsOnly](#PsOnly) | قيمة تحدد شرطًا لاستخدام خطوط PostScript فقط. |
### Default {#Default}
```
public static final byte Default
```


قيمة تحدد السلوك الافتراضي.

### String {#String}
```
public static final byte String
```


قيمة تُرجع عند تعداد الخطوط المرسومة.

### Stroke {#Stroke}
```
public static final byte Stroke
```


قيمة تُرجع عند تعداد خطوط TrueType وغيرها من الخطوط المتجهة وخطوط المتجه.

### Tt {#Tt}
```
public static final byte Tt
```


قيمة تحدد اختيار خط TrueType عندما يحتوي النظام على عدة خطوط بنفس الاسم.

### Device {#Device}
```
public static final byte Device
```


قيمة تحدد اختيار خط الجهاز عندما يحتوي النظام على عدة خطوط بنفس الاسم.

### Raster {#Raster}
```
public static final byte Raster
```


قيمة تحدد اختيار خط مرسوم عندما يحتوي النظام على عدة خطوط بنفس الاسم.

### TtOnly {#TtOnly}
```
public static final byte TtOnly
```


قيمة تحدد شرطًا لاستخدام خطوط TrueType فقط. إذا لم تكن هناك خطوط TrueType مثبتة في النظام، يتم تحديد السلوك الافتراضي.

### Outline {#Outline}
```
public static final byte Outline
```


قيمة تحدد المتطلب لخطوط TrueType وغيرها من الخطوط المتجهة.

### ScreenOutline {#ScreenOutline}
```
public static final byte ScreenOutline
```


قيمة تحدد تفضيلًا لخطوط TrueType وغيرها من الخطوط المتجهة.

### PsOnly {#PsOnly}
```
public static final byte PsOnly
```


قيمة تحدد شرطًا لاستخدام خطوط PostScript فقط. إذا لم تكن هناك خطوط PostScript مثبتة في النظام، يتم تحديد السلوك الافتراضي.

