---
title: "تعداد PngCompressionLevel"
second_title: "Aspose.Imaging for .NET API Reference"
description: "تعداد Aspose.Imaging.ImageOptions.PngCompressionLevel. تعداد مستوى ضغط png"
type: docs
weight: 10500
url: /ar/net/aspose.imaging.imageoptions/pngcompressionlevel/
---
## PngCompressionLevel enumeration

تعداد مستوى ضغط png.

```csharp
public enum PngCompressionLevel
```

### القيم

| الاسم | القيمة | الوصف |
| --- | --- | --- |
| ZipLevel0 | `0` | سيتم تخزين البيانات ببساطة، دون أي تغيير. يستخدم تنفيذًا أبطأ لتقنية deflate مع مقياس ضغط. |
| ZipLevel1 | `1` | أسرع ضغط لكنه الأقل فعالية. يستخدم تنفيذًا أبطأ لتقنية deflate مع مقياس ضغط. |
| ZipLevel2 | `2` | أبطأ قليلًا، لكن أفضل، من المستوى 1. يستخدم تنفيذًا أبطأ لتقنية deflate مع مقياس ضغط. |
| ZipLevel3 | `3` | أبطأ قليلًا، لكن أفضل، من المستوى 2. يستخدم تنفيذًا أبطأ لتقنية deflate مع مقياس ضغط. |
| ZipLevel4 | `4` | أبطأ قليلًا، لكن أفضل، من المستوى 3. يستخدم تنفيذًا أبطأ لتقنية deflate مع مقياس ضغط. |
| ZipLevel5 | `5` | أبطأ قليلًا من المستوى 4، لكن مع ضغط أفضل. يستخدم تنفيذًا أبطأ لتقنية deflate مع مقياس ضغط. |
| ZipLevel6 | `6` | أبطأ قليلًا من المستوى 5، لكن مع ضغط أفضل. يستخدم تنفيذًا أبطأ لتقنية deflate مع مقياس ضغط. |
| ZipLevel7 | `7` | ضغط أفضل من المستوى 6، لكن أبطأ أكثر. يستخدم تنفيذًا أبطأ لتقنية deflate مع مقياس ضغط. |
| ZipLevel8 | `8` | ضغط أفضل من المستوى 7، لكن أبطأ أكثر. يستخدم تنفيذًا أبطأ لتقنية deflate مع مقياس ضغط. |
| ZipLevel9 | `9` | ضغط \"الأفضل\"، حيث يعني الأفضل أكبر تقليل في حجم تدفق البيانات المدخلة. هذا أيضًا هو أبطأ ضغط. يستخدم تنفيذًا أبطأ لتقنية deflate مع مقياس ضغط. |
| DeflateRecomended | `10` | أكثر ضغط مُحسّن، مع توازن جيد بين السرعة وكفاءة الضغط. يستخدم تنفيذًا أسرع لتقنية deflate دون مقياس ضغط. |

### انظر أيضًا

* namespace [Aspose.Imaging.ImageOptions](../../aspose.imaging.imageoptions/)
* assembly [Aspose.Imaging](../../)


