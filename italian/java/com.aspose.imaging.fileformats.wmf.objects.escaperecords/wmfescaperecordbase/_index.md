---
title: "WmfEscapeRecordBase"
second_title: "Riferimento API Aspose.Imaging per Java"
description: "La base del record escape."
type: docs
weight: 12
url: /it/java/com.aspose.imaging.fileformats.wmf.objects.escaperecords/wmfescaperecordbase/
---
**Inheritance:**
java.lang.Object, [com.aspose.imaging.fileformats.emf.MetaObject](../../com.aspose.imaging/fileformats.emf/metaobject), [com.aspose.imaging.fileformats.wmf.objects.WmfObject](../../com.aspose.imaging/fileformats.wmf.objects/wmfobject)
```
public class WmfEscapeRecordBase extends WmfObject
```

La base del record escape.
## Costruttori

| Costruttore | Descrizione |
| --- | --- |
| [WmfEscapeRecordBase()](#WmfEscapeRecordBase--) |  |
## Metodi

| Metodo | Descrizione |
| --- | --- |
| [getByteCount()](#getByteCount--) | Recupera o imposta il conteggio dei byte. |
| [setByteCount(int value)](#setByteCount-int-) | Recupera o imposta il conteggio dei byte. |
| [getData()](#getData--) | Ottiene o imposta i dati. |
| [setData(byte[] value)](#setData-byte---) | Ottiene o imposta i dati. |
| [getChecked()](#getChecked--) | Recupera un valore che indica se questo `WmfEscapeRecordBase` è selezionato. |
| [setChecked(boolean value)](#setChecked-boolean-) | Recupera un valore che indica se questo `WmfEscapeRecordBase` è selezionato. |
### WmfEscapeRecordBase() {#WmfEscapeRecordBase--}
```
public WmfEscapeRecordBase()
```


### getByteCount() {#getByteCount--}
```
public int getByteCount()
```


Recupera o imposta il conteggio dei byte.

Valore: Un intero senza segno a 16 bit che specifica la dimensione, in byte, dei dati del record che segue. Questo valore DEVE essere 34 più il valore del campo EnhancedMetafileDataSize.

**Returns:**
int
### setByteCount(int value) {#setByteCount-int-}
```
public void setByteCount(int value)
```


Recupera o imposta il conteggio dei byte.

Valore: Un intero senza segno a 16 bit che specifica la dimensione, in byte, dei dati del record che segue. Questo valore DEVE essere 34 più il valore del campo EnhancedMetafileDataSize.

**Parameters:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| valore | int |  |

### getData() {#getData--}
```
public byte[] getData()
```


Ottiene o imposta i dati.

Valore: I dati.

**Returns:**
byte[]
### setData(byte[] value) {#setData-byte---}
```
public void setData(byte[] value)
```


Ottiene o imposta i dati.

Valore: I dati.

**Parameters:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| valore | byte[] |  |

### getChecked() {#getChecked--}
```
public boolean getChecked()
```


Recupera un valore che indica se questo `WmfEscapeRecordBase` è selezionato.

Valore: `true` se selezionato; altrimenti, `false`.

**Returns:**
boolean
### setChecked(boolean value) {#setChecked-boolean-}
```
public void setChecked(boolean value)
```


Recupera un valore che indica se questo `WmfEscapeRecordBase` è selezionato.

Valore: `true` se selezionato; altrimenti, `false`.

**Parameters:**
| Parametro | Tipo | Descrizione |
| --- | --- | --- |
| valore | boolean |  |

