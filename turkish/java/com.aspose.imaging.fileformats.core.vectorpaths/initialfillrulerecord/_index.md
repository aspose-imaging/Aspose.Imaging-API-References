---
title: "InitialFillRuleRecord"
second_title: "Aspose.Imaging for Java API Referansı"
description: "İlk Doldurma Kuralı Kayıt Sınıfı"
type: docs
weight: 12
url: /tr/java/com.aspose.imaging.fileformats.core.vectorpaths/initialfillrulerecord/
---
**Inheritance:**
java.lang.Object, [com.aspose.imaging.fileformats.core.vectorpaths.VectorPathRecord](../../com.aspose.imaging.fileformats.core.vectorpaths/vectorpathrecord)
```
public class InitialFillRuleRecord extends VectorPathRecord
```

İlk Doldurma Kuralı Kayıt Sınıfı
## Yapıcılar

| Yapıcı | Açıklama |
| --- | --- |
| [InitialFillRuleRecord()](#InitialFillRuleRecord--) | Yeni bir [InitialFillRuleRecord](../../com.aspose.imaging.fileformats.core.vectorpaths/initialfillrulerecord) sınıfının örneğini başlatır. |
| [InitialFillRuleRecord(boolean isFillStartsWithAllPixels)](#InitialFillRuleRecord-boolean-) | Yeni bir [InitialFillRuleRecord](../../com.aspose.imaging.fileformats.core.vectorpaths/initialfillrulerecord) sınıfının örneğini başlatır. |
| [InitialFillRuleRecord(byte[] data)](#InitialFillRuleRecord-byte---) | Yeni bir [InitialFillRuleRecord](../../com.aspose.imaging.fileformats.core.vectorpaths/initialfillrulerecord) sınıfının örneğini başlatır. |
## Yöntemler

| Yöntem | Açıklama |
| --- | --- |
| [isFillStartsWithAllPixels()](#isFillStartsWithAllPixels--) | is fill starts with all pixels özelliğini gösteren bir değeri alır. |
| [setFillStartsWithAllPixels(boolean value)](#setFillStartsWithAllPixels-boolean-) | is fill starts with all pixels özelliğini gösteren bir değeri ayarlar. |
| [getType()](#getType--) | Türü alır. |
### InitialFillRuleRecord() {#InitialFillRuleRecord--}
```
public InitialFillRuleRecord()
```


Yeni bir [InitialFillRuleRecord](../../com.aspose.imaging.fileformats.core.vectorpaths/initialfillrulerecord) sınıfının örneğini başlatır.

### InitialFillRuleRecord(boolean isFillStartsWithAllPixels) {#InitialFillRuleRecord-boolean-}
```
public InitialFillRuleRecord(boolean isFillStartsWithAllPixels)
```


Yeni bir [InitialFillRuleRecord](../../com.aspose.imaging.fileformats.core.vectorpaths/initialfillrulerecord) sınıfının örneğini başlatır.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| isFillStartsWithAllPixels | boolean | Bu is fill starts with all pixels. |

### InitialFillRuleRecord(byte[] data) {#InitialFillRuleRecord-byte---}
```
public InitialFillRuleRecord(byte[] data)
```


Yeni bir [InitialFillRuleRecord](../../com.aspose.imaging.fileformats.core.vectorpaths/initialfillrulerecord) sınıfının örneğini başlatır.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| veri | byte[] | Kayıt verileri. |

### isFillStartsWithAllPixels() {#isFillStartsWithAllPixels--}
```
public final boolean isFillStartsWithAllPixels()
```


is fill starts with all pixels özelliğini gösteren bir değeri alır.

Değer: Bu is fill starts with all pixels.

**Returns:**
boolean - is fill starts with all pixels özelliğini gösteren bir değer.
### setFillStartsWithAllPixels(boolean value) {#setFillStartsWithAllPixels-boolean-}
```
public final void setFillStartsWithAllPixels(boolean value)
```


is fill starts with all pixels özelliğini gösteren bir değeri ayarlar.

Değer: Bu is fill starts with all pixels.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| değer | boolean | tüm piksellerle doldurmanın başlayıp başlamadığını gösteren bir değer. |

### getType() {#getType--}
```
public short getType()
```


Türü alır.

Değer: Tür.

**Returns:**
short - tür.
