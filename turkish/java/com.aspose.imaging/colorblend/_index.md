---
title: "ColorBlend"
second_title: "Aspose.Imaging for Java API Referansı"
description: "Çok renkli bir geçişte renk karışımını ara değerleme için kullanılan renk ve konum dizilerini tanımlar."
type: docs
weight: 22
url: /tr/java/com.aspose.imaging/colorblend/
---
**Inheritance:**
java.lang.Object
```
public final class ColorBlend
```

Çok renkli bir geçişte renk karışımını ara değerleme için kullanılan renk ve konum dizilerini tanımlar. Bu sınıf kalıtılamaz.
## Yapıcılar

| Yapıcı | Açıklama |
| --- | --- |
| [ColorBlend()](#ColorBlend--) | `com.aspose.imaging.ColorBlend` sınıfının yeni bir örneğini başlatır. |
| [ColorBlend(int count)](#ColorBlend-int-) | `com.aspose.imaging.ColorBlend` sınıfının belirtilen renk ve konum sayısıyla yeni bir örneğini başlatır. |
## Yöntemler

| Yöntem | Açıklama |
| --- | --- |
| [getColors()](#getColors--) | Bir gradyan boyunca karşılık gelen konumlardaki kullanılacak renkleri temsil eden renklerin bir dizisini alır veya ayarlar. |
| [setColors(Color[] value)](#setColors-com.aspose.imaging.Color---) |  |
| [getPositions()](#getPositions--) | Bir gradyan çizgisi boyunca konumları alır veya ayarlar. |
| [setPositions(float[] value)](#setPositions-float---) |  |
| [equals(Object obj)](#equals-java.lang.Object-) | Belirtilen nesnenin bir `com.aspose.imaging.ColorBlend` sınıfı olup olmadığını ve bu `com.aspose.imaging.ColorBlend` sınıfına eşdeğer olup olmadığını test eder. |
| [hashCode()](#hashCode--) | Bu örnek için bir karma kodu döndürür. |
### ColorBlend() {#ColorBlend--}
```
public ColorBlend()
```


`com.aspose.imaging.ColorBlend` sınıfının yeni bir örneğini başlatır.

### ColorBlend(int count) {#ColorBlend-int-}
```
public ColorBlend(int count)
```


`com.aspose.imaging.ColorBlend` sınıfının belirtilen renk ve konum sayısıyla yeni bir örneğini başlatır.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| count | int | Bu `com.aspose.imaging.ColorBlend` içindeki renk ve konum sayısı. |

### getColors() {#getColors--}
```
public Color[] getColors()
```


Bir gradyan boyunca karşılık gelen konumlardaki kullanılacak renkleri temsil eden renklerin bir dizisini alır veya ayarlar.

**Returns:**
com.aspose.imaging.Color[] - Bir gradyan boyunca karşılık gelen konumlardaki kullanılacak renkleri temsil eden `com.aspose.imaging.Color` yapılarının bir dizisi.
### setColors(Color[] value) {#setColors-com.aspose.imaging.Color---}
```
public void setColors(Color[] value)
```




**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| value | [Color\[\]](../../com.aspose.imaging/color) |  |

### getPositions() {#getPositions--}
```
public float[] getPositions()
```


Bir gradyan çizgisi boyunca konumları alır veya ayarlar.

**Returns:**
float[] - Gradyan çizgisi boyunca mesafenin yüzdelerini belirten değerlerin bir dizisi.
### setPositions(float[] value) {#setPositions-float---}
```
public void setPositions(float[] value)
```




**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| değer | float[] |  |

### equals(Object obj) {#equals-java.lang.Object-}
```
public boolean equals(Object obj)
```


Belirtilen nesnenin bir `com.aspose.imaging.ColorBlend` sınıfı olup olmadığını ve bu `com.aspose.imaging.ColorBlend` sınıfına eşdeğer olup olmadığını test eder.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| obj | java.lang.Object | Test edilecek nesne. |

**Returns:**
boolean - `obj` bu `com.aspose.imaging.ColorBlend` sınıfına eşdeğer bir `com.aspose.imaging.ColorBlend` sınıfı ise doğru; aksi takdirde yanlış.
### hashCode() {#hashCode--}
```
public int hashCode()
```


Bu örnek için bir karma kodu döndürür.

**Returns:**
int - Bu örnek için bir karma kodu, karma algoritmaları ve hash tablosu gibi veri yapılarında kullanılmaya uygundur.
