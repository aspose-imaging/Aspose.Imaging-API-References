---
title: "XmpChoise"
second_title: "Aspose.Imaging for Java API Referansı"
description: "Değer sözlüğünden bir değeri temsil eder."
type: docs
weight: 13
url: /tr/java/com.aspose.imaging.xmp.types.derived/xmpchoise/
---
**Inheritance:**
java.lang.Object, [com.aspose.imaging.xmp.types.XmpTypeBase](../../com.aspose.imaging.xmp.types/xmptypebase)
```
public class XmpChoise<T> extends XmpTypeBase
```

Değer sözlüğünden bir değeri temsil eder.
## Yapıcılar

| Yapıcı | Açıklama |
| --- | --- |
| [XmpChoise()](#XmpChoise--) | Yeni bir `XmpChoise\{T\}` sınıfı örneği oluşturur. |
## Yöntemler

| Yöntem | Açıklama |
| --- | --- |
| [isOpen()](#isOpen--) | Tercih edilen değerlerin genişletilebileceğini gösteren bir değeri alır veya ayarlar. |
| [setOpen(boolean value)](#setOpen-boolean-) | Tercih edilen değerlerin genişletilebileceğini gösteren bir değeri alır veya ayarlar. |
| [getXmpRepresentation()](#getXmpRepresentation--) | XMP formatında içerilen dize değerini alır. |
### XmpChoise() {#XmpChoise--}
```
public XmpChoise()
```


Yeni bir `XmpChoise\{T\}` sınıfı örneği oluşturur.

### isOpen() {#isOpen--}
```
public boolean isOpen()
```


Tercih edilen değerlerin genişletilebileceğini gösteren bir değeri alır veya ayarlar.

Değer: `true` eğer tercih edilen değerler genişletilebiliyorsa; aksi takdirde `false`.

**Returns:**
boolean
### setOpen(boolean value) {#setOpen-boolean-}
```
public void setOpen(boolean value)
```


Tercih edilen değerlerin genişletilebileceğini gösteren bir değeri alır veya ayarlar.

Değer: `true` eğer tercih edilen değerler genişletilebiliyorsa; aksi takdirde `false`.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| değer | boolean |  |

### getXmpRepresentation() {#getXmpRepresentation--}
```
public String getXmpRepresentation()
```


XMP formatında içerilen dize değerini alır.

**Returns:**
java.lang.String - XMP biçiminde içerilen dize değerini döndürür.
