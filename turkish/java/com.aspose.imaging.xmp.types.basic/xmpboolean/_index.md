---
title: "XmpBoolean"
second_title: "Aspose.Imaging for Java API Referansı"
description: "XMP Boolean temel tipini temsil eder."
type: docs
weight: 10
url: /tr/java/com.aspose.imaging.xmp.types.basic/xmpboolean/
---
**Inheritance:**
java.lang.Object, [com.aspose.imaging.xmp.types.XmpTypeBase](../../com.aspose.imaging.xmp.types/xmptypebase)
```
public final class XmpBoolean extends XmpTypeBase
```

XMP Boolean temel tipini temsil eder.
## Yapıcılar

| Yapıcı | Açıklama |
| --- | --- |
| [XmpBoolean(boolean value)](#XmpBoolean-boolean-) | Boolean değere dayalı yeni bir `XmpBoolean` sınıfı örneği başlatır. |
| [XmpBoolean()](#XmpBoolean--) | Varsayılan değerle yeni bir `XmpBoolean` sınıfı örneği başlatır. |
| [XmpBoolean(String value)](#XmpBoolean-java.lang.String-) | Yeni bir `XmpBoolean` sınıfı örneği başlatır. |
## Yöntemler

| Yöntem | Açıklama |
| --- | --- |
| [getValue()](#getValue--) | Bu `XmpBoolean`'ın değer olup olmadığını gösteren bir değeri alır veya ayarlar. |
| [setValue(boolean value)](#setValue-boolean-) | Bu `XmpBoolean`'ın değer olup olmadığını gösteren bir değeri alır veya ayarlar. |
| [getXmpRepresentation()](#getXmpRepresentation--) | XMP formatında içeriği olan dizeyi döndürür. |
### XmpBoolean(boolean value) {#XmpBoolean-boolean-}
```
public XmpBoolean(boolean value)
```


Boolean değere dayalı yeni bir `XmpBoolean` sınıfı örneği başlatır.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| değer | boolean | Boolean değeri. İzin verilen değerler True veya False. |

### XmpBoolean() {#XmpBoolean--}
```
public XmpBoolean()
```


Varsayılan değerle yeni bir `XmpBoolean` sınıfı örneği başlatır.

### XmpBoolean(String value) {#XmpBoolean-java.lang.String-}
```
public XmpBoolean(String value)
```


Yeni bir `XmpBoolean` sınıfı örneği başlatır.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| değer | java.lang.String | Değer. |

### getValue() {#getValue--}
```
public boolean getValue()
```


Bu `XmpBoolean`'ın değer olup olmadığını gösteren bir değeri alır veya ayarlar.

Değer: Değer ise `true`; aksi takdirde `false`.

**Returns:**
boolean
### setValue(boolean value) {#setValue-boolean-}
```
public void setValue(boolean value)
```


Bu `XmpBoolean`'ın değer olup olmadığını gösteren bir değeri alır veya ayarlar.

Değer: Değer ise `true`; aksi takdirde `false`.

**Parameters:**
| Parametre | Tür | Açıklama |
| --- | --- | --- |
| değer | boolean |  |

### getXmpRepresentation() {#getXmpRepresentation--}
```
public String getXmpRepresentation()
```


XMP formatında içeriği olan dizeyi döndürür.

**Returns:**
java.lang.String - XMP temsili içeren dizeyi döndürür.
