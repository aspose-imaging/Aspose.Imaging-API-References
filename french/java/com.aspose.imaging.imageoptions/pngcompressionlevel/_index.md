---
title: "PngCompressionLevel"
second_title: "Référence de l'API Aspose.Imaging pour Java"
description: "L'énumération des niveaux de compression PNG."
type: docs
weight: 37
url: /fr/java/com.aspose.imaging.imageoptions/pngcompressionlevel/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class PngCompressionLevel extends System.Enum
```

L'énumération des niveaux de compression PNG.
## Champs

| Champ | Description |
| --- | --- |
| [ZipLevel0](#ZipLevel0) | Les données seront simplement stockées, sans aucune modification. |
| [ZipLevel1](#ZipLevel1) | La compression la plus rapide mais la moins efficace. |
| [ZipLevel2](#ZipLevel2) | Un peu plus lente, mais meilleure, que le niveau 1. |
| [ZipLevel3](#ZipLevel3) | Un peu plus lente, mais meilleure, que le niveau 2. |
| [ZipLevel4](#ZipLevel4) | Un peu plus lente, mais meilleure, que le niveau 3. |
| [ZipLevel5](#ZipLevel5) | Un peu plus lente que le niveau 4, mais avec une meilleure compression. |
| [ZipLevel6](#ZipLevel6) | Un peu plus lente que le niveau 5, mais avec une meilleure compression. |
| [ZipLevel7](#ZipLevel7) | Une meilleure compression que le niveau 6, mais encore plus lente. |
| [ZipLevel8](#ZipLevel8) | Une meilleure compression que le niveau 7, mais encore plus lente. |
| [ZipLevel9](#ZipLevel9) | La compression "meilleure", où "meilleure" signifie la plus grande réduction de la taille du flux de données d'entrée. |
| [DeflateRecommended](#DeflateRecommended) | La compression la plus optimisée, avec un bon équilibre entre vitesse et efficacité de compression. |
### ZipLevel0 {#ZipLevel0}
```
public static final int ZipLevel0
```


Les données seront simplement stockées, sans aucune modification. Utilise une implémentation deflate plus lente avec une échelle de compression.

### ZipLevel1 {#ZipLevel1}
```
public static final int ZipLevel1
```


La compression la plus rapide mais la moins efficace. Utilise une implémentation deflate plus lente avec une échelle de compression.

### ZipLevel2 {#ZipLevel2}
```
public static final int ZipLevel2
```


Un peu plus lente, mais meilleure, que le niveau 1. Utilise une implémentation deflate plus lente avec une échelle de compression.

### ZipLevel3 {#ZipLevel3}
```
public static final int ZipLevel3
```


Un peu plus lente, mais meilleure, que le niveau 2. Utilise une implémentation deflate plus lente avec une échelle de compression.

### ZipLevel4 {#ZipLevel4}
```
public static final int ZipLevel4
```


Un peu plus lente, mais meilleure, que le niveau 3. Utilise une implémentation deflate plus lente avec une échelle de compression.

### ZipLevel5 {#ZipLevel5}
```
public static final int ZipLevel5
```


Un peu plus lente que le niveau 4, mais avec une meilleure compression. Utilise une implémentation deflate plus lente avec une échelle de compression.

### ZipLevel6 {#ZipLevel6}
```
public static final int ZipLevel6
```


Un peu plus lente que le niveau 5, mais avec une meilleure compression. Utilise une implémentation deflate plus lente avec une échelle de compression.

### ZipLevel7 {#ZipLevel7}
```
public static final int ZipLevel7
```


Une meilleure compression que le niveau 6, mais encore plus lente. Utilise une implémentation deflate plus lente avec une échelle de compression.

### ZipLevel8 {#ZipLevel8}
```
public static final int ZipLevel8
```


Une meilleure compression que le niveau 7, mais encore plus lente. Utilise une implémentation deflate plus lente avec une échelle de compression.

### ZipLevel9 {#ZipLevel9}
```
public static final int ZipLevel9
```


La compression "meilleure", où "meilleure" signifie la plus grande réduction de la taille du flux de données d'entrée. C'est également la compression la plus lente. Utilise une implémentation deflate plus lente avec une échelle de compression.

### DeflateRecommended {#DeflateRecommended}
```
public static final int DeflateRecommended
```


La compression la plus optimisée, avec un bon équilibre entre vitesse et efficacité de compression. Utilise une implémentation deflate plus rapide sans échelle de compression.

