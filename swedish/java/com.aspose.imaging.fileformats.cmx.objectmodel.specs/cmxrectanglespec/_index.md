---
title: "CmxRectangleSpec"
second_title: "Aspose.Imaging för Java API-referens"
description: "Representerar geometrisk information specificerad för en rektangel."
type: docs
weight: 16
url: /sv/java/com.aspose.imaging.fileformats.cmx.objectmodel.specs/cmxrectanglespec/
---
**Inheritance:**
java.lang.Object

**All Implemented Interfaces:**
[com.aspose.imaging.fileformats.cmx.objectmodel.specs.ICmxObjectSpec](../../com.aspose.imaging.fileformats.cmx.objectmodel.specs/icmxobjectspec)
```
public class CmxRectangleSpec implements ICmxObjectSpec
```

Representerar geometrisk information specificerad för en rektangel.
## Konstruktörer

| Konstruktor | Beskrivning |
| --- | --- |
| [CmxRectangleSpec()](#CmxRectangleSpec--) |  |
## Metoder

| Metod | Beskrivning |
| --- | --- |
| [getCenterX()](#getCenterX--) | Hämtar X-koordinaten för rektangelns centrum. |
| [setCenterX(float value)](#setCenterX-float-) | Ställer in X-koordinaten för rektangelns centrum. |
| [getCenterY()](#getCenterY--) | Hämtar Y-koordinaten för rektangelns centrum. |
| [setCenterY(float value)](#setCenterY-float-) | Ställer in Y-koordinaten för rektangelns centrum. |
| [getWidth()](#getWidth--) | Hämtar rektangelns bredd. |
| [setWidth(float value)](#setWidth-float-) | Ställer in rektangelns bredd. |
| [getHeight()](#getHeight--) | Hämtar rektangelns höjd. |
| [setHeight(float value)](#setHeight-float-) | Ställer in rektangelns höjd. |
| [getRadius()](#getRadius--) | Hämtar radien för de rundade hörnen på rektangeln. |
| [setRadius(float value)](#setRadius-float-) | Ställer in radien för de rundade hörnen på rektangeln. |
| [getAngle()](#getAngle--) | Hämtar rotationsvinkeln för rektangeln. |
| [setAngle(float value)](#setAngle-float-) | Ställer in rotationsvinkeln för rektangeln. |
| [toString()](#toString--) | Returnerar en String som representerar detta objekt. |
| [equals(Object o)](#equals-java.lang.Object-) | Kontrollera om objekt är lika. |
| [hashCode()](#hashCode--) | Hämta hashkoden för det aktuella objektet. |
### CmxRectangleSpec() {#CmxRectangleSpec--}
```
public CmxRectangleSpec()
```


### getCenterX() {#getCenterX--}
```
public final float getCenterX()
```


Hämtar X-koordinaten för rektangelns centrum. Mäts i vanliga dokumentavståndsenheter.

**Returns:**
float - X-koordinaten för rektangelns centrum.
### setCenterX(float value) {#setCenterX-float-}
```
public final void setCenterX(float value)
```


Ställer in X-koordinaten för rektangelns centrum. Mäts i vanliga dokumentavståndsenheter.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | float | X-koordinaten för rektangelns centrum. |

### getCenterY() {#getCenterY--}
```
public final float getCenterY()
```


Hämtar Y‑koordinaten för rektangelns centrum. Mäts i vanliga dokumentavståndsenheter.

**Returns:**
float - Y‑koordinaten för rektangelns centrum.
### setCenterY(float value) {#setCenterY-float-}
```
public final void setCenterY(float value)
```


Ställer in Y‑koordinaten för rektangelns centrum. Mäts i vanliga dokumentavståndsenheter.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | float | Y‑koordinaten för rektangelns centrum. |

### getWidth() {#getWidth--}
```
public final float getWidth()
```


Hämtar rektangelns bredd. Mäts i vanliga dokumentavståndsenheter.

**Returns:**
float - rektangelns bredd.
### setWidth(float value) {#setWidth-float-}
```
public final void setWidth(float value)
```


Ställer in rektangelns bredd. Mäts i vanliga dokumentavståndsenheter.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | float | rektangelns bredd. |

### getHeight() {#getHeight--}
```
public final float getHeight()
```


Hämtar rektangelns höjd. Mäts i vanliga dokumentavståndsenheter.

**Returns:**
float - rektangelns höjd.
### setHeight(float value) {#setHeight-float-}
```
public final void setHeight(float value)
```


Ställer in rektangelns höjd. Mäts i vanliga dokumentavståndsenheter.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | float | rektangelns höjd. |

### getRadius() {#getRadius--}
```
public final float getRadius()
```


Hämtar radien för de avrundade rektangelhörnen. Om dess värde är `0` har rektangeln inga avrundade hörn. Mäts i vanliga dokumentavståndsenheter.

**Returns:**
float - radien för de avrundade rektangelhörnen.
### setRadius(float value) {#setRadius-float-}
```
public final void setRadius(float value)
```


Ställer in radien för de avrundade rektangelhörnen. Om dess värde är `0` har rektangeln inga avrundade hörn. Mäts i vanliga dokumentavståndsenheter.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | float | radien för de avrundade rektangelhörnen. |

### getAngle() {#getAngle--}
```
public final float getAngle()
```


Hämtar rotationsvinkeln för rektangeln. Mäts i radianer.

**Returns:**
float - rotationsvinkeln för rektangeln.
### setAngle(float value) {#setAngle-float-}
```
public final void setAngle(float value)
```


Ställer in rotationsvinkeln för rektangeln. Mäts i radianer.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | float | rotationsvinkeln för rektangeln. |

### toString() {#toString--}
```
public String toString()
```


Returnerar en String som representerar detta objekt.

**Returns:**
java.lang.String - En sträng som representerar detta objekt.
### equals(Object o) {#equals-java.lang.Object-}
```
public boolean equals(Object o)
```


Kontrollera om objekt är lika.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| o | java.lang.Object | Det andra objektet. |

**Returns:**
boolean - Resultatet av likhetsjämförelsen.
### hashCode() {#hashCode--}
```
public int hashCode()
```


Hämta hashkoden för det aktuella objektet.

**Returns:**
int - Hashkoden.
