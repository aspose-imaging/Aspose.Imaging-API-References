---
title: "BezierKnotRecord"
second_title: "Aspose.Imaging för Java API-referens"
description: "Bezier Knot Record Class"
type: docs
weight: 10
url: /sv/java/com.aspose.imaging.fileformats.core.vectorpaths/bezierknotrecord/
---
**Inheritance:**
java.lang.Object, [com.aspose.imaging.fileformats.core.vectorpaths.VectorPathRecord](../../com.aspose.imaging.fileformats.core.vectorpaths/vectorpathrecord)
```
public class BezierKnotRecord extends VectorPathRecord
```

Bezier Knot Record Class
## Konstruktörer

| Konstruktor | Beskrivning |
| --- | --- |
| [BezierKnotRecord()](#BezierKnotRecord--) | Initierar en ny instans av klassen [BezierKnotRecord](../../com.aspose.imaging.fileformats.core.vectorpaths/bezierknotrecord). |
| [BezierKnotRecord(byte[] data)](#BezierKnotRecord-byte---) | Initierar en ny instans av klassen [BezierKnotRecord](../../com.aspose.imaging.fileformats.core.vectorpaths/bezierknotrecord). |
## Metoder

| Metod | Beskrivning |
| --- | --- |
| [getPathPoints()](#getPathPoints--) | Hämtar banpunkterna. |
| [setPathPoints(PointF[] value)](#setPathPoints-com.aspose.imaging.PointF---) | Ställer in sökvägspunkterna. |
| [getPoints()](#getPoints--) | Hämtar punkterna. |
| [setPoints(Point[] value)](#setPoints-com.aspose.imaging.Point---) | Ställer in punkterna. |
| [isClosed()](#isClosed--) | Hämtar ett värde som indikerar om detta objekt är stängt. |
| [setClosed(boolean value)](#setClosed-boolean-) | Ställer in ett värde som indikerar om detta objekt är stängt. |
| [isLinked()](#isLinked--) | Hämtar ett värde som indikerar om detta objekt är länkat. |
| [setLinked(boolean value)](#setLinked-boolean-) | Ställer in ett värde som indikerar om detta objekt är länkat. |
| [isOpen()](#isOpen--) | Hämtar ett värde som indikerar om detta objekt är öppet. |
| [setOpen(boolean value)](#setOpen-boolean-) | Ställer in ett värde som indikerar om detta objekt är öppet. |
| [getType()](#getType--) | Hämtar typen. |
### BezierKnotRecord() {#BezierKnotRecord--}
```
public BezierKnotRecord()
```


Initierar en ny instans av klassen [BezierKnotRecord](../../com.aspose.imaging.fileformats.core.vectorpaths/bezierknotrecord).

### BezierKnotRecord(byte[] data) {#BezierKnotRecord-byte---}
```
public BezierKnotRecord(byte[] data)
```


Initierar en ny instans av klassen [BezierKnotRecord](../../com.aspose.imaging.fileformats.core.vectorpaths/bezierknotrecord).

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| data | byte[] | Postdata. |

### getPathPoints() {#getPathPoints--}
```
public final PointF[] getPathPoints()
```


Hämtar banpunkterna.

Värde: Sökvägspunkterna.

**Returns:**
com.aspose.imaging.PointF[] - sökvägspunkterna.
### setPathPoints(PointF[] value) {#setPathPoints-com.aspose.imaging.PointF---}
```
public final void setPathPoints(PointF[] value)
```


Ställer in sökvägspunkterna.

Värde: Sökvägspunkterna.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | [PointF\[\]](../../com.aspose.imaging/pointf) | sökvägspunkterna. |

### getPoints() {#getPoints--}
```
public final Point[] getPoints()
```


Hämtar punkterna.

**Returns:**
com.aspose.imaging.Point[] - punkterna.
### setPoints(Point[] value) {#setPoints-com.aspose.imaging.Point---}
```
public final void setPoints(Point[] value)
```


Ställer in punkterna.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| value | [Point\[\]](../../com.aspose.imaging/point) | punkterna. |

### isClosed() {#isClosed--}
```
public final boolean isClosed()
```


Hämtar ett värde som indikerar om detta objekt är stängt.

Värde: `true` om detta objekt är stängt; annars `false`.

**Returns:**
boolean - ett värde som indikerar om detta objekt är stängt.
### setClosed(boolean value) {#setClosed-boolean-}
```
public final void setClosed(boolean value)
```


Ställer in ett värde som indikerar om detta objekt är stängt.

Värde: `true` om detta objekt är stängt; annars `false`.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | boolean | ett värde som indikerar om detta objekt är stängt. |

### isLinked() {#isLinked--}
```
public final boolean isLinked()
```


Hämtar ett värde som indikerar om detta objekt är länkat.

Värde: `true` om detta objekt är länkat; annars `false`.

**Returns:**
boolean - ett värde som indikerar om detta objekt är länkat.
### setLinked(boolean value) {#setLinked-boolean-}
```
public final void setLinked(boolean value)
```


Ställer in ett värde som indikerar om detta objekt är länkat.

Värde: `true` om detta objekt är länkat; annars `false`.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | boolean | ett värde som indikerar om detta objekt är länkat. |

### isOpen() {#isOpen--}
```
public final boolean isOpen()
```


Hämtar ett värde som indikerar om detta objekt är öppet.

Värde: `true` om detta objekt är öppet; annars `false`.

**Returns:**
boolean - ett värde som indikerar om detta objekt är öppet.
### setOpen(boolean value) {#setOpen-boolean-}
```
public final void setOpen(boolean value)
```


Ställer in ett värde som indikerar om detta objekt är öppet.

Värde: `true` om detta objekt är öppet; annars `false`.

**Parameters:**
| Parameter | Typ | Beskrivning |
| --- | --- | --- |
| värde | boolean | ett värde som indikerar om detta objekt är öppet. |

### getType() {#getType--}
```
public short getType()
```


Hämtar typen.

Värde: Typen.

**Returns:**
short - typen.
