---
title: WmfCompression
second_title: Aspose.Imaging para la referencia de la API de .NET
description: La enumeración de compresión especifica el tipo de compresión para una imagen de mapa de bits
type: docs
weight: 8150
url: /es/aspose.imaging.fileformats.wmf.consts/wmfcompression/
---
## WmfCompression enumeration

La enumeración de compresión especifica el tipo de compresión para una imagen de mapa de bits

```csharp
public enum WmfCompression
```

### Valores

| Nombre | Valor | Descripción |
| --- | --- | --- |
| BI_RGB | `0` | El mapa de bits está en formato rojo verde azul (RGB) sin comprimir que no está comprimido y no usa máscaras de color. |
| BI_RLE8 | `1` | Un formato RGB que usa compresión de codificación de longitud de ejecución (RLE) para mapas de bits con 8 bits por píxel. La compresión usa un formato de 2 bytes que consta de un byte de conteo seguido de un byte que contiene un índice de color. |
| BI_RLE4 | `2` | Un formato RGB que usa compresión RLE para mapas de bits con 4 bits por píxel. La compresión usa un formato de 2 bytes que consta de un byte de conteo seguido de dos índices de color de longitud de palabra |
| BI_BITFIELDS | `3` | El mapa de bits no está comprimido y la tabla de colores consta de tres máscaras de color DWORD que especifican los componentes rojo, verde y azul, respectivamente, de cada píxel. Esto es válido cuando se utiliza con mapas de bits de 16 y 32 bits por píxel. |
| BI_JPEG | `4` | La imagen es una imagen JPEG, como se especifica en [JFIF]. Este valor solo DEBE usarse en ciertas operaciones bitmap , como el paso de JPEG. La aplicación DEBE consultar la compatibilidad con el paso a través, ya que no todos los dispositivos admiten el paso a través de JPEG. El uso de mapas de bits que no sean RGB PUEDE limitar la portabilidad del metarchivo a otros dispositivos. Por ejemplo, los contextos de dispositivos de visualización generalmente no admiten este pass-through |
| BI_PNG | `5` | La imagen es una imagen PNG, como se especifica en [RFC2083]. Este valor DEBE utilizarse únicamente en determinadas operaciones de mapa de bits, , como la transferencia de archivos JPEG/PNG. La aplicación DEBE consultar la compatibilidad con la transferencia, ya que no todos los dispositivos admiten la transferencia JPEG/PNG. El uso de mapas de bits que no sean RGB PUEDE limitar la portabilidad del metarchivo a otros dispositivos. Por ejemplo, los contextos de dispositivos de visualización generalmente no admiten este paso. |
| BI_CMYK | `11` | La imagen es un formato CMYK sin comprimir. |
| BI_CMYKRLE8 | `12` | Un formato CMYK que usa compresión RLE para mapas de bits con 8 bits por píxel. La compresión usa un formato de 2 bytes que consta de un byte de conteo seguido de un byte que contiene un índice de color. |
| BI_CMYKRLE4 | `13` | Un formato CMYK que usa compresión RLE para mapas de bits con 4 bits por píxel. La compresión usa un formato de 2 bytes que consta de un byte de conteo seguido de dos índices de color de longitud de palabra. |

### Ver también

* espacio de nombres [Aspose.Imaging.FileFormats.Wmf.Consts](../../aspose.imaging.fileformats.wmf.consts)
* asamblea [Aspose.Imaging](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Imaging.dll -->
