---
title: "PenAlignment"
second_title: "Aspose.Imaging for Java API 参考文档"
description: "指定 Pen 对象相对于理论零宽线的对齐方式。"
type: docs
weight: 82
url: /zh/java/com.aspose.imaging/penalignment/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class PenAlignment extends System.Enum
```

指定 `Pen` 对象相对于理论的零宽线的对齐方式。
## 字段

| 字段 | 描述 |
| --- | --- |
| [Center](#Center) | 指定 `Pen` 对象位于理论线的中心。 |
| [Inset](#Inset) | 指定 `Pen` 位于理论线的内部。 |
| [Outset](#Outset) | 指定 `Pen` 位于理论线的外部。 |
| [Left](#Left) | 指定 `Pen` 位于理论线的左侧。 |
| [Right](#Right) | 指定 `Pen` 位于理论线的右侧。 |
### Center {#Center}
```
public static final int Center
```


指定 `Pen` 对象位于理论线的中心。

### Inset {#Inset}
```
public static final int Inset
```


指定 `Pen` 位于理论线的内部。

### Outset {#Outset}
```
public static final int Outset
```


指定 `Pen` 位于理论线的外部。

### Left {#Left}
```
public static final int Left
```


指定 `Pen` 位于理论线的左侧。

### Right {#Right}
```
public static final int Right
```


指定 `Pen` 位于理论线的右侧。

