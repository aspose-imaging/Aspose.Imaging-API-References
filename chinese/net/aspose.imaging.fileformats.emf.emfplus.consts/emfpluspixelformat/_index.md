---
title: "枚举 EmfPlusPixelFormat"
second_title: "Aspose.Imaging for .NET API 参考"
description: "Aspose.Imaging.FileFormats.Emf.EmfPlus.Consts.EmfPlusPixelFormat 枚举。PixelFormat 枚举定义了 EMF 位图支持的像素格式。"
type: docs
weight: 5130
url: /zh/net/aspose.imaging.fileformats.emf.emfplus.consts/emfpluspixelformat/
---
## EmfPlusPixelFormat enumeration

PixelFormat 枚举定义了 EMF+ 位图支持的像素格式。

```csharp
public enum EmfPlusPixelFormat
```

### 值

| 名称 | 值 | 描述 |
| --- | --- | --- |
| PixelFormatUndefined | `0` | 未指定格式。 |
| PixelFormat1bppIndexed | `196865` | 该格式为单色，并使用颜色调色板查找表。 |
| PixelFormat4bppIndexed | `197634` | 该格式为 16 色，并使用颜色调色板查找表。 |
| PixelFormat8bppIndexed | `198659` | 该格式为 256 色，并使用颜色调色板查找表。 |
| PixelFormat16bppGrayScale | `1052676` | 该格式为每像素 16 位，灰度。 |
| PixelFormat16bppRGB555 | `135173` | 该格式为每像素 16 位；红、绿、蓝分量各使用 5 位。剩余的位未使用。 |
| PixelFormat16bppRGB565 | `135174` | 该格式为每像素 16 位；红色分量使用 5 位，绿色分量使用 6 位，蓝色分量使用 5 位。 |
| PixelFormat16bppARGB1555 | `397319` | 该格式为每像素 16 位；Alpha 分量使用 1 位，红、绿、蓝分量各使用 5 位。 |
| PixelFormat24bppRGB | `137224` | 该格式为每像素 24 位；每个颜色分量（红、绿、蓝）各使用 8 位。 |
| PixelFormat32bppRGB | `139273` | 该格式为每像素 32 位；每个颜色分量（红、绿、蓝）各使用 8 位。剩余的 8 位未使用。 |
| PixelFormat32bppARGB | `2498570` | 该格式为每像素 32 位；每个分量（Alpha、红、绿、蓝）各使用 8 位。 |
| PixelFormat32bppPARGB | `925707` | 该格式为每像素 32 位；每个分量（Alpha、红、绿、蓝）各使用 8 位。红、绿、蓝分量根据 Alpha 分量进行预乘。 |
| PixelFormat48bppRGB | `1060876` | 该格式为每像素 48 位；每个颜色分量（红、绿、蓝）各使用 16 位。 |
| PixelFormat64bppARGB | `3424269` | 该格式为每像素 64 位；每个分量（Alpha、红、绿、蓝）各使用 16 位。 |
| PixelFormat64bppPARGB | `1720334` | 该格式为每像素 64 位；每个分量（Alpha、红、绿、蓝）各使用 16 位。红、绿、蓝分量根据 Alpha 分量进行预乘。 |

### 另请参见

* namespace [Aspose.Imaging.FileFormats.Emf.EmfPlus.Consts](../../aspose.imaging.fileformats.emf.emfplus.consts/)
* assembly [Aspose.Imaging](../../)


