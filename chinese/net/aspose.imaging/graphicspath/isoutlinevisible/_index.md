---
title: "GraphicsPath.IsOutlineVisible"
second_title: "Aspose.Imaging for .NET API 参考"
description: "GraphicsPath 方法。指示在使用指定笔绘制时，指定点是否位于此 GraphicsPath 的轮廓内部。"
type: docs
weight: 130
url: /zh/net/aspose.imaging/graphicspath/isoutlinevisible/
---
## IsOutlineVisible(float, float, Pen) {#isoutlinevisible_6}

指示在使用指定[`Pen`](../../pen/)绘制时，指定点是否位于此[`GraphicsPath`](../)的轮廓内部（下方）。

```csharp
public bool IsOutlineVisible(float x, float y, Pen pen)
```

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| x | 单精度 | 要测试的点的 x 坐标。 |
| y | 单精度 | 要测试的点的 y 坐标。 |
| pen | Pen | 用于测试的[`Pen`](../../pen/)。 |

### 返回值

如果在使用指定[`Pen`](../../pen/)绘制时，指定点位于此[`GraphicsPath`](../)的轮廓内部，则此方法返回 true；否则返回 false。

### 另请参见

* class [Pen](../../pen/)
* class [GraphicsPath](../)
* namespace [Aspose.Imaging](../../graphicspath/)
* assembly [Aspose.Imaging](../../../)

---

## IsOutlineVisible(PointF, Pen) {#isoutlinevisible_2}

指示在使用指定[`Pen`](../../pen/)绘制时，指定点是否位于此[`GraphicsPath`](../)的轮廓内部（下方）。

```csharp
public bool IsOutlineVisible(PointF point, Pen pen)
```

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| point | PointF | 一个指定要测试位置的 [`PointF`](../../pointf/)。 |
| pen | Pen | 用于测试的[`Pen`](../../pen/)。 |

### 返回值

如果在使用指定[`Pen`](../../pen/)绘制时，指定点位于此[`GraphicsPath`](../)的轮廓内部，则此方法返回 true；否则返回 false。

### 另请参见

* struct [PointF](../../pointf/)
* class [Pen](../../pen/)
* class [GraphicsPath](../)
* namespace [Aspose.Imaging](../../graphicspath/)
* assembly [Aspose.Imaging](../../../)

---

## IsOutlineVisible(float, float, Pen, Graphics) {#isoutlinevisible_7}

指示使用指定的 [`Pen`](../../pen/) 绘制并使用指定的 [`Graphics`](../../graphics/) 时，指定的点是否位于此 [`GraphicsPath`](../) 的轮廓内部（下方）。

```csharp
public bool IsOutlineVisible(float x, float y, Pen pen, Graphics graphics)
```

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| x | 单精度 | 要测试的点的 x 坐标。 |
| y | 单精度 | 要测试的点的 y 坐标。 |
| pen | Pen | 用于测试的[`Pen`](../../pen/)。 |
| graphics | Graphics | 用于测试可见性的 [`Graphics`](../../graphics/)。 |

### 返回值

如果使用指定的 [`Pen`](../../pen/) 绘制此 [`GraphicsPath`](../) 时，指定的点位于其轮廓内部（下方），则此方法返回 true；否则返回 false。

### 另请参见

* class [Pen](../../pen/)
* class [Graphics](../../graphics/)
* class [GraphicsPath](../)
* namespace [Aspose.Imaging](../../graphicspath/)
* assembly [Aspose.Imaging](../../../)

---

## IsOutlineVisible(PointF, Pen, Graphics) {#isoutlinevisible_3}

指示使用指定的 [`Pen`](../../pen/) 绘制并使用指定的 [`Graphics`](../../graphics/) 时，指定的点是否位于此 [`GraphicsPath`](../) 的轮廓内部（下方）。

```csharp
public bool IsOutlineVisible(PointF pt, Pen pen, Graphics graphics)
```

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| pt | PointF | 一个指定要测试位置的 [`PointF`](../../pointf/)。 |
| pen | Pen | 用于测试的[`Pen`](../../pen/)。 |
| graphics | Graphics | 用于测试可见性的 [`Graphics`](../../graphics/)。 |

### 返回值

如果使用指定的 [`Pen`](../../pen/) 绘制此 [`GraphicsPath`](../) 时，指定的点位于其轮廓内部（下方），则此方法返回 true；否则返回 false。

### 另请参见

* struct [PointF](../../pointf/)
* class [Pen](../../pen/)
* class [Graphics](../../graphics/)
* class [GraphicsPath](../)
* namespace [Aspose.Imaging](../../graphicspath/)
* assembly [Aspose.Imaging](../../../)

---

## IsOutlineVisible(int, int, Pen) {#isoutlinevisible_4}

指示在使用指定[`Pen`](../../pen/)绘制时，指定点是否位于此[`GraphicsPath`](../)的轮廓内部（下方）。

```csharp
public bool IsOutlineVisible(int x, int y, Pen pen)
```

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| x | Int32 | 要测试的点的 x 坐标。 |
| y | Int32 | 要测试的点的 y 坐标。 |
| pen | Pen | 用于测试的[`Pen`](../../pen/)。 |

### 返回值

如果在使用指定[`Pen`](../../pen/)绘制时，指定点位于此[`GraphicsPath`](../)的轮廓内部，则此方法返回 true；否则返回 false。

### 另请参见

* class [Pen](../../pen/)
* class [GraphicsPath](../)
* namespace [Aspose.Imaging](../../graphicspath/)
* assembly [Aspose.Imaging](../../../)

---

## IsOutlineVisible(Point, Pen) {#isoutlinevisible}

指示在使用指定[`Pen`](../../pen/)绘制时，指定点是否位于此[`GraphicsPath`](../)的轮廓内部（下方）。

```csharp
public bool IsOutlineVisible(Point point, Pen pen)
```

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| point | Point | 一个指定要测试位置的 [`Point`](../../point/)。 |
| pen | Pen | 用于测试的[`Pen`](../../pen/)。 |

### 返回值

如果在使用指定[`Pen`](../../pen/)绘制时，指定点位于此[`GraphicsPath`](../)的轮廓内部，则此方法返回 true；否则返回 false。

### 另请参见

* struct [Point](../../point/)
* class [Pen](../../pen/)
* class [GraphicsPath](../)
* namespace [Aspose.Imaging](../../graphicspath/)
* assembly [Aspose.Imaging](../../../)

---

## IsOutlineVisible(int, int, Pen, Graphics) {#isoutlinevisible_5}

指示使用指定的 [`Pen`](../../pen/) 绘制并使用指定的 [`Graphics`](../../graphics/) 时，指定的点是否位于此 [`GraphicsPath`](../) 的轮廓内部（下方）。

```csharp
public bool IsOutlineVisible(int x, int y, Pen pen, Graphics graphics)
```

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| x | Int32 | 要测试的点的 x 坐标。 |
| y | Int32 | 要测试的点的 y 坐标。 |
| pen | Pen | 用于测试的[`Pen`](../../pen/)。 |
| graphics | Graphics | 用于测试可见性的 [`Graphics`](../../graphics/)。 |

### 返回值

如果使用指定的 [`Pen`](../../pen/) 绘制此 [`GraphicsPath`](../) 时，指定的点位于其轮廓内部，则此方法返回 true；否则返回 false。

### 另请参见

* class [Pen](../../pen/)
* class [Graphics](../../graphics/)
* class [GraphicsPath](../)
* namespace [Aspose.Imaging](../../graphicspath/)
* assembly [Aspose.Imaging](../../../)

---

## IsOutlineVisible(Point, Pen, Graphics) {#isoutlinevisible_1}

指示使用指定的 [`Pen`](../../pen/) 绘制并使用指定的 [`Graphics`](../../graphics/) 时，指定的点是否位于此 [`GraphicsPath`](../) 的轮廓内部（下方）。

```csharp
public bool IsOutlineVisible(Point pt, Pen pen, Graphics graphics)
```

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| pt | Point | 一个指定要测试位置的 [`Point`](../../point/)。 |
| pen | Pen | 用于测试的[`Pen`](../../pen/)。 |
| graphics | Graphics | 用于测试可见性的 [`Graphics`](../../graphics/)。 |

### 返回值

如果使用指定的 [`Pen`](../../pen/) 绘制此 [`GraphicsPath`](../) 时，指定的点位于其轮廓内部，则此方法返回 true；否则返回 false。

### 另请参见

* struct [Point](../../point/)
* class [Pen](../../pen/)
* class [Graphics](../../graphics/)
* class [GraphicsPath](../)
* namespace [Aspose.Imaging](../../graphicspath/)
* assembly [Aspose.Imaging](../../../)


