---
title: "枚举 PngCompressionLevel"
second_title: "Aspose.Imaging for .NET API 参考"
description: "Aspose.Imaging.ImageOptions.PngCompressionLevel 枚举。png 压缩级别枚举"
type: docs
weight: 10500
url: /zh/net/aspose.imaging.imageoptions/pngcompressionlevel/
---
## PngCompressionLevel enumeration

PNG 压缩级别枚举。

```csharp
public enum PngCompressionLevel
```

### 值

| 名称 | 值 | 描述 |
| --- | --- | --- |
| ZipLevel0 | `0` | 数据将被简单存储，完全不做更改。使用较慢的 deflate 实现并带有压缩比例。 |
| ZipLevel1 | `1` | 最快但效果最差的压缩。使用较慢的 deflate 实现并带有压缩比例。 |
| ZipLevel2 | `2` | 比级别 1 稍慢，但更好。使用较慢的 deflate 实现并带有压缩比例。 |
| ZipLevel3 | `3` | 比级别 2 稍慢，但更好。使用较慢的 deflate 实现并带有压缩比例。 |
| ZipLevel4 | `4` | 比级别 3 稍慢，但更好。使用较慢的 deflate 实现并带有压缩比例。 |
| ZipLevel5 | `5` | 比级别 4 稍慢，但压缩效果更好。使用较慢的 deflate 实现并带有压缩比例。 |
| ZipLevel6 | `6` | 比级别 5 稍慢，但压缩效果更好。使用较慢的 deflate 实现并带有压缩比例。 |
| ZipLevel7 | `7` | 比级别 6 更好的压缩，但更慢。使用较慢的 deflate 实现并带有压缩比例。 |
| ZipLevel8 | `8` | 比级别 7 更好的压缩，但更慢。使用较慢的 deflate 实现并带有压缩比例。 |
| ZipLevel9 | `9` | “最佳”压缩，其中最佳指对输入数据流大小的最大缩减。这也是最慢的压缩。使用较慢的 deflate 实现并带有压缩比例。 |
| DeflateRecomended | `10` | 最优化的压缩，在速度和压缩效率之间取得良好平衡。使用更快的 deflate 实现且不带压缩比例。 |

### 另请参见

* namespace [Aspose.Imaging.ImageOptions](../../aspose.imaging.imageoptions/)
* assembly [Aspose.Imaging](../../)


