---
title: "枚举 TiffExpectedFormat"
second_title: "Aspose.Imaging for .NET API 参考"
description: "Aspose.Imaging.FileFormats.Tiff.Enums.TiffExpectedFormat 枚举。预期的 Tiff 文件格式"
type: docs
weight: 7750
url: /zh/net/aspose.imaging.fileformats.tiff.enums/tiffexpectedformat/
---
## TiffExpectedFormat enumeration

预期的 Tiff 文件格式。

```csharp
public enum TiffExpectedFormat
```

### 值

| 名称 | 值 | 描述 |
| --- | --- | --- |
| Default | `0` | 默认的 TIFF 格式是不压缩，仅限 B/W 每像素 1 位格式。您也可以使用此设置获取空选项，并使用您的标签或其他设置进行初始化。 |
| TiffLzwBw | `1` | 采用 LZW 压缩且仅限 B/W 每像素 1 位格式的 TIFF。 |
| TiffLzwRgb | `2` | 采用 LZW 压缩且为 RGB 彩色格式的 TIFF。 |
| TiffLzwRgba | `3` | 采用 LZW 压缩且为带透明度的 ARGB 彩色格式的 TIFF。 |
| TiffLzwCmyk | `4` | 采用 LZW 压缩且为 CMYK 颜色模式的 TIFF。 |
| TiffLzwCmyka | `5` | 采用 LZW 压缩且为带透明度的 CMYKA 颜色模式的 TIFF。 |
| TiffCcittFax3 | `6` | 采用 CCITT FAX3 编码的 TIFF。仅支持该方案的 B/W 每像素 1 位格式。 |
| TiffCcittFax4 | `7` | 采用 CCITT FAX4 编码的 TIFF。仅支持该方案的 B/W 每像素 1 位格式。 |
| TiffDeflateBw | `8` | 采用 Deflate 压缩且仅限 B/W 每像素 1 位格式的 TIFF。 |
| TiffDeflateRgb | `9` | 采用 Deflate 压缩且为 RGB 彩色格式的 TIFF。 |
| TiffDeflateRgba | `10` | 采用 Deflate 压缩且为 ARGB 彩色格式的 TIFF。 |
| TiffDeflateCmyk | `11` | 采用 Deflate 压缩且为 CMYK 彩色格式的 TIFF。 |
| TiffDeflateCmyka | `12` | 采用 Deflate 压缩且为带透明度的 CMYKA 彩色格式的 TIFF。 |
| TiffCcitRle | `13` | 采用 CCITT RLE 编码的 TIFF。仅支持该方案的 B/W 每像素 1 位格式。 |
| TiffJpegRgb | `14` | 采用 Jpeg 压缩且为 RGB 彩色格式的 TIFF。 |
| TiffJpegYCbCr | `15` | 采用 Jpeg 压缩且为 YCBCR 彩色格式的 TIFF。 |
| TiffNoCompressionBw | `16` | 未压缩的 TIFF，采用仅限 B/W 每像素 1 位格式。 |
| TiffNoCompressionRgb | `17` | 未压缩的 TIFF，采用 RGB 彩色格式。 |
| TiffNoCompressionRgba | `18` | 未压缩的 TIFF，采用带透明度的 ARGB 彩色格式。 |
| TiffNoCompressionCmyk | `19` | 未压缩的 TIFF，采用 CMYK 彩色格式。 |
| TiffNoCompressionCmyka | `20` | 未压缩的 TIFF，采用带透明度的 CMYK 彩色格式。 |

### 另请参见

* namespace [Aspose.Imaging.FileFormats.Tiff.Enums](../../aspose.imaging.fileformats.tiff.enums/)
* assembly [Aspose.Imaging](../../)


