---
title: "PngCompressionLevel"
second_title: "Справочник API Aspose.Imaging для Java"
description: "Перечисление уровней сжатия PNG."
type: docs
weight: 37
url: /ru/java/com.aspose.imaging.imageoptions/pngcompressionlevel/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class PngCompressionLevel extends System.Enum
```

Перечисление уровней сжатия PNG.
## Поля

| Поле | Описание |
| --- | --- |
| [ZipLevel0](#ZipLevel0) | Данные будут просто сохранены без каких-либо изменений. |
| [ZipLevel1](#ZipLevel1) | Самое быстрое, но наименее эффективное сжатие. |
| [ZipLevel2](#ZipLevel2) | Немного медленнее, но лучше, чем уровень 1. |
| [ZipLevel3](#ZipLevel3) | Немного медленнее, но лучше, чем уровень 2. |
| [ZipLevel4](#ZipLevel4) | Немного медленнее, но лучше, чем уровень 3. |
| [ZipLevel5](#ZipLevel5) | Немного медленнее, чем уровень 4, но с лучшим сжатием. |
| [ZipLevel6](#ZipLevel6) | Немного медленнее, чем уровень 5, но с лучшим сжатием. |
| [ZipLevel7](#ZipLevel7) | Лучшее сжатие, чем у уровня 6, но еще медленнее. |
| [ZipLevel8](#ZipLevel8) | Лучшее сжатие, чем у уровня 7, но еще медленнее. |
| [ZipLevel9](#ZipLevel9) | Самое "лучшее" сжатие, где "лучшее" означает наибольшее уменьшение размера входного потока данных. |
| [DeflateRecommended](#DeflateRecommended) | Самое оптимизированное сжатие с хорошим балансом скорости и эффективности сжатия. |
### ZipLevel0 {#ZipLevel0}
```
public static final int ZipLevel0
```


Данные будут просто сохранены без каких-либо изменений. Используется более медленная реализация deflate с масштабом сжатия.

### ZipLevel1 {#ZipLevel1}
```
public static final int ZipLevel1
```


Самое быстрое, но наименее эффективное сжатие. Используется более медленная реализация deflate с масштабом сжатия.

### ZipLevel2 {#ZipLevel2}
```
public static final int ZipLevel2
```


Немного медленнее, но лучше, чем уровень 1. Используется более медленная реализация deflate с масштабом сжатия.

### ZipLevel3 {#ZipLevel3}
```
public static final int ZipLevel3
```


Немного медленнее, но лучше, чем уровень 2. Используется более медленная реализация deflate с масштабом сжатия.

### ZipLevel4 {#ZipLevel4}
```
public static final int ZipLevel4
```


Немного медленнее, но лучше, чем уровень 3. Используется более медленная реализация deflate с масштабом сжатия.

### ZipLevel5 {#ZipLevel5}
```
public static final int ZipLevel5
```


Немного медленнее, чем уровень 4, но с лучшим сжатием. Используется более медленная реализация deflate с масштабом сжатия.

### ZipLevel6 {#ZipLevel6}
```
public static final int ZipLevel6
```


Немного медленнее, чем уровень 5, но с лучшим сжатием. Используется более медленная реализация deflate с масштабом сжатия.

### ZipLevel7 {#ZipLevel7}
```
public static final int ZipLevel7
```


Лучшее сжатие, чем у уровня 6, но еще медленнее. Используется более медленная реализация deflate с масштабом сжатия.

### ZipLevel8 {#ZipLevel8}
```
public static final int ZipLevel8
```


Лучшее сжатие, чем у уровня 7, но еще медленнее. Используется более медленная реализация deflate с масштабом сжатия.

### ZipLevel9 {#ZipLevel9}
```
public static final int ZipLevel9
```


Самое "лучшее" сжатие, где "лучшее" означает наибольшее уменьшение размера входного потока данных. Это также самое медленное сжатие. Используется более медленная реализация deflate с масштабом сжатия.

### DeflateRecommended {#DeflateRecommended}
```
public static final int DeflateRecommended
```


Самое оптимизированное сжатие с хорошим балансом скорости и эффективности сжатия. Используется более быстрая реализация deflate без масштаба сжатия.

