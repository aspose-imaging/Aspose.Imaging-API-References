---
title: "BezierKnotRecord"
second_title: "Aspose.Imaging für Java API-Referenz"
description: "Bezier-Knoten-Aufzeichnungs-Klasse"
type: docs
weight: 10
url: /de/java/com.aspose.imaging.fileformats.core.vectorpaths/bezierknotrecord/
---
**Inheritance:**
java.lang.Object, [com.aspose.imaging.fileformats.core.vectorpaths.VectorPathRecord](../../com.aspose.imaging.fileformats.core.vectorpaths/vectorpathrecord)
```
public class BezierKnotRecord extends VectorPathRecord
```

Bezier-Knoten-Aufzeichnungs-Klasse
## Konstruktoren

| Konstruktor | Beschreibung |
| --- | --- |
| [BezierKnotRecord()](#BezierKnotRecord--) | Initialisiert eine neue Instanz der [BezierKnotRecord](../../com.aspose.imaging.fileformats.core.vectorpaths/bezierknotrecord) Klasse. |
| [BezierKnotRecord(byte[] data)](#BezierKnotRecord-byte---) | Initialisiert eine neue Instanz der [BezierKnotRecord](../../com.aspose.imaging.fileformats.core.vectorpaths/bezierknotrecord) Klasse. |
## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getPathPoints()](#getPathPoints--) | Liest die Pfadpunkte. |
| [setPathPoints(PointF[] value)](#setPathPoints-com.aspose.imaging.PointF---) | Setzt die Pfadpunkte. |
| [getPoints()](#getPoints--) | Liest die Punkte. |
| [setPoints(Point[] value)](#setPoints-com.aspose.imaging.Point---) | Setzt die Punkte. |
| [isClosed()](#isClosed--) | Liefert einen Wert, der angibt, ob diese Instanz geschlossen ist. |
| [setClosed(boolean value)](#setClosed-boolean-) | Setzt einen Wert, der angibt, ob diese Instanz geschlossen ist. |
| [isLinked()](#isLinked--) | Liefert einen Wert, der angibt, ob diese Instanz verknüpft ist. |
| [setLinked(boolean value)](#setLinked-boolean-) | Setzt einen Wert, der angibt, ob diese Instanz verknüpft ist. |
| [isOpen()](#isOpen--) | Liefert einen Wert, der angibt, ob diese Instanz geöffnet ist. |
| [setOpen(boolean value)](#setOpen-boolean-) | Setzt einen Wert, der angibt, ob diese Instanz geöffnet ist. |
| [getType()](#getType--) | Liest den Typ. |
### BezierKnotRecord() {#BezierKnotRecord--}
```
public BezierKnotRecord()
```


Initialisiert eine neue Instanz der [BezierKnotRecord](../../com.aspose.imaging.fileformats.core.vectorpaths/bezierknotrecord) Klasse.

### BezierKnotRecord(byte[] data) {#BezierKnotRecord-byte---}
```
public BezierKnotRecord(byte[] data)
```


Initialisiert eine neue Instanz der [BezierKnotRecord](../../com.aspose.imaging.fileformats.core.vectorpaths/bezierknotrecord) Klasse.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Daten | byte[] | Die Daten des Datensatzes. |

### getPathPoints() {#getPathPoints--}
```
public final PointF[] getPathPoints()
```


Liest die Pfadpunkte.

Wert: Die Pfadpunkte.

**Returns:**
com.aspose.imaging.PointF[] - die Pfadpunkte.
### setPathPoints(PointF[] value) {#setPathPoints-com.aspose.imaging.PointF---}
```
public final void setPathPoints(PointF[] value)
```


Setzt die Pfadpunkte.

Wert: Die Pfadpunkte.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | [PointF\[\]](../../com.aspose.imaging/pointf) | die Pfadpunkte. |

### getPoints() {#getPoints--}
```
public final Point[] getPoints()
```


Liest die Punkte.

**Returns:**
com.aspose.imaging.Point[] - die Punkte.
### setPoints(Point[] value) {#setPoints-com.aspose.imaging.Point---}
```
public final void setPoints(Point[] value)
```


Setzt die Punkte.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| value | [Point\[\]](../../com.aspose.imaging/point) | die Punkte. |

### isClosed() {#isClosed--}
```
public final boolean isClosed()
```


Liefert einen Wert, der angibt, ob diese Instanz geschlossen ist.

Wert: `true`, wenn diese Instanz geschlossen ist; andernfalls `false`.

**Returns:**
boolean - ein Wert, der angibt, ob diese Instanz geschlossen ist.
### setClosed(boolean value) {#setClosed-boolean-}
```
public final void setClosed(boolean value)
```


Setzt einen Wert, der angibt, ob diese Instanz geschlossen ist.

Wert: `true`, wenn diese Instanz geschlossen ist; andernfalls `false`.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | boolean | ein Wert, der angibt, ob diese Instanz geschlossen ist. |

### isLinked() {#isLinked--}
```
public final boolean isLinked()
```


Liefert einen Wert, der angibt, ob diese Instanz verknüpft ist.

Wert: `true`, wenn diese Instanz verknüpft ist; andernfalls `false`.

**Returns:**
boolean - ein Wert, der angibt, ob diese Instanz verknüpft ist.
### setLinked(boolean value) {#setLinked-boolean-}
```
public final void setLinked(boolean value)
```


Setzt einen Wert, der angibt, ob diese Instanz verknüpft ist.

Wert: `true`, wenn diese Instanz verknüpft ist; andernfalls `false`.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | boolean | ein Wert, der angibt, ob diese Instanz verknüpft ist. |

### isOpen() {#isOpen--}
```
public final boolean isOpen()
```


Liefert einen Wert, der angibt, ob diese Instanz geöffnet ist.

Wert: `true`, wenn diese Instanz geöffnet ist; andernfalls `false`.

**Returns:**
boolean - ein Wert, der angibt, ob diese Instanz geöffnet ist.
### setOpen(boolean value) {#setOpen-boolean-}
```
public final void setOpen(boolean value)
```


Setzt einen Wert, der angibt, ob diese Instanz geöffnet ist.

Wert: `true`, wenn diese Instanz geöffnet ist; andernfalls `false`.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | boolean | ein Wert, der angibt, ob diese Instanz geöffnet ist. |

### getType() {#getType--}
```
public short getType()
```


Liest den Typ.

Wert: Der Typ.

**Returns:**
short - der Typ.
