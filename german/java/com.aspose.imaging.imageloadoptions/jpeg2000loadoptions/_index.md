---
title: "Jpeg2000LoadOptions"
second_title: "Aspose.Imaging für Java API-Referenz"
description: "JPEG2000-Ladeoptionen"
type: docs
weight: 13
url: /de/java/com.aspose.imaging.imageloadoptions/jpeg2000loadoptions/
---
**Inheritance:**
java.lang.Object, [com.aspose.imaging.LoadOptions](../../com.aspose.imaging/loadoptions)
```
public class Jpeg2000LoadOptions extends LoadOptions
```

JPEG2000-Ladeoptionen
## Konstruktoren

| Konstruktor | Beschreibung |
| --- | --- |
| [Jpeg2000LoadOptions()](#Jpeg2000LoadOptions--) | Initialisiert eine neue Instanz der `Jpeg2000LoadOptions`-Klasse. |
## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getMaximumDecodingTime()](#getMaximumDecodingTime--) | Liefert die maximale Dekodierzeit in Sekunden (diese Option kann auf sehr langsamen, speicherarmen Maschinen verwendet werden, um ein Hängenbleiben beim Verarbeiten sehr großer Bilder zu verhindern – Auflösung größer als 5500 × 6500 Pixel). |
| [setMaximumDecodingTime(int value)](#setMaximumDecodingTime-int-) | Setzt die maximale Dekodierzeit in Sekunden (diese Option kann auf sehr langsamen, speicherarmen Maschinen verwendet werden, um ein Hängenbleiben beim Verarbeiten sehr großer Bilder zu verhindern – Auflösung größer als 5500 × 6500 Pixel). |
| [getMaximumDecodingTimeForTile()](#getMaximumDecodingTimeForTile--) | Liefert die maximale Dekodierzeit für Kachel. |
| [setMaximumDecodingTimeForTile(int value)](#setMaximumDecodingTimeForTile-int-) | Setzt die maximale Dekodierzeit für Kachel. |
### Jpeg2000LoadOptions() {#Jpeg2000LoadOptions--}
```
public Jpeg2000LoadOptions()
```


Initialisiert eine neue Instanz der `Jpeg2000LoadOptions`-Klasse.

### getMaximumDecodingTime() {#getMaximumDecodingTime--}
```
public int getMaximumDecodingTime()
```


Liefert die maximale Dekodierzeit in Sekunden (diese Option kann auf sehr langsamen, speicherarmen Maschinen verwendet werden, um ein Hängenbleiben beim Verarbeiten sehr großer Bilder zu verhindern – Auflösung größer als 5500 × 6500 Pixel).

**Returns:**
int – Die maximale Dekodierzeit.
### setMaximumDecodingTime(int value) {#setMaximumDecodingTime-int-}
```
public void setMaximumDecodingTime(int value)
```


Setzt die maximale Dekodierzeit in Sekunden (diese Option kann auf sehr langsamen, speicherarmen Maschinen verwendet werden, um ein Hängenbleiben beim Verarbeiten sehr großer Bilder zu verhindern – Auflösung größer als 5500 × 6500 Pixel).

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | int | Die maximale Dekodierzeit. |

### getMaximumDecodingTimeForTile() {#getMaximumDecodingTimeForTile--}
```
public final int getMaximumDecodingTimeForTile()
```


Liefert die maximale Dekodierzeit für Kachel.

Wert: Die maximale Dekodierzeit für Kachel.

**Returns:**
int – die maximale Dekodierzeit für Kachel.
### setMaximumDecodingTimeForTile(int value) {#setMaximumDecodingTimeForTile-int-}
```
public final void setMaximumDecodingTimeForTile(int value)
```


Setzt die maximale Dekodierzeit für Kachel.

Wert: Die maximale Dekodierzeit für Kachel.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | int | die maximale Dekodierzeit für Kachel. |

