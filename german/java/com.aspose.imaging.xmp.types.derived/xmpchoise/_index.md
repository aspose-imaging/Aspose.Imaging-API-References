---
title: "XmpChoise"
second_title: "Aspose.Imaging für Java API-Referenz"
description: "Stellt einen Wert aus einem Vokabular von Werten dar."
type: docs
weight: 13
url: /de/java/com.aspose.imaging.xmp.types.derived/xmpchoise/
---
**Inheritance:**
java.lang.Object, [com.aspose.imaging.xmp.types.XmpTypeBase](../../com.aspose.imaging.xmp.types/xmptypebase)
```
public class XmpChoise<T> extends XmpTypeBase
```

Stellt einen Wert aus einem Vokabular von Werten dar.
## Konstruktoren

| Konstruktor | Beschreibung |
| --- | --- |
| [XmpChoise()](#XmpChoise--) | Initialisiert eine neue Instanz der `XmpChoise\{T\}`-Klasse. |
## Methoden

| Methode | Beschreibung |
| --- | --- |
| [isOpen()](#isOpen--) | Liefert oder setzt einen Wert, der angibt, ob die bevorzugten Werte erweitert werden können. |
| [setOpen(boolean value)](#setOpen-boolean-) | Liefert oder setzt einen Wert, der angibt, ob die bevorzugten Werte erweitert werden können. |
| [getXmpRepresentation()](#getXmpRepresentation--) | Liest den enthaltenen Zeichenfolgenwert im XMP‑Format. |
### XmpChoise() {#XmpChoise--}
```
public XmpChoise()
```


Initialisiert eine neue Instanz der `XmpChoise\{T\}`-Klasse.

### isOpen() {#isOpen--}
```
public boolean isOpen()
```


Liefert oder setzt einen Wert, der angibt, ob die bevorzugten Werte erweitert werden können.

Wert: `true`, wenn bevorzugte Werte erweitert werden können; andernfalls `false`.

**Returns:**
boolean
### setOpen(boolean value) {#setOpen-boolean-}
```
public void setOpen(boolean value)
```


Liefert oder setzt einen Wert, der angibt, ob die bevorzugten Werte erweitert werden können.

Wert: `true`, wenn bevorzugte Werte erweitert werden können; andernfalls `false`.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | boolean |  |

### getXmpRepresentation() {#getXmpRepresentation--}
```
public String getXmpRepresentation()
```


Liest den enthaltenen Zeichenfolgenwert im XMP‑Format.

**Returns:**
java.lang.String - Gibt den im String enthaltenen Wert im XMP-Format zurück.
