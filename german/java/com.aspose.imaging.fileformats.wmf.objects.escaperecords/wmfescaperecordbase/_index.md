---
title: "WmfEscapeRecordBase"
second_title: "Aspose.Imaging für Java API-Referenz"
description: "Die Escape‑Datensatz‑Basis."
type: docs
weight: 12
url: /de/java/com.aspose.imaging.fileformats.wmf.objects.escaperecords/wmfescaperecordbase/
---
**Inheritance:**
java.lang.Object, [com.aspose.imaging.fileformats.emf.MetaObject](../../com.aspose.imaging.fileformats.emf/metaobject), [com.aspose.imaging.fileformats.wmf.objects.WmfObject](../../com.aspose.imaging.fileformats.wmf.objects/wmfobject)
```
public class WmfEscapeRecordBase extends WmfObject
```

Die Escape‑Datensatz‑Basis.
## Konstruktoren

| Konstruktor | Beschreibung |
| --- | --- |
| [WmfEscapeRecordBase()](#WmfEscapeRecordBase--) |  |
## Methoden

| Methode | Beschreibung |
| --- | --- |
| [getByteCount()](#getByteCount--) | Ruft die Byte‑Anzahl ab oder legt sie fest. |
| [setByteCount(int value)](#setByteCount-int-) | Ruft die Byte‑Anzahl ab oder legt sie fest. |
| [getData()](#getData--) | Liest oder setzt die Daten. |
| [setData(byte[] value)](#setData-byte---) | Liest oder setzt die Daten. |
| [getChecked()](#getChecked--) | Ruft einen Wert ab, der angibt, ob dieses `WmfEscapeRecordBase` aktiviert ist. |
| [setChecked(boolean value)](#setChecked-boolean-) | Ruft einen Wert ab, der angibt, ob dieses `WmfEscapeRecordBase` aktiviert ist. |
### WmfEscapeRecordBase() {#WmfEscapeRecordBase--}
```
public WmfEscapeRecordBase()
```


### getByteCount() {#getByteCount--}
```
public int getByteCount()
```


Ruft die Byte‑Anzahl ab oder legt sie fest.

Wert: Ein 16‑Bit vorzeichenloser Integer, der die Größe der folgenden Datensatz‑Daten in Bytes angibt. Dieser Wert MUSS 34 plus den Wert des Feldes EnhancedMetafileDataSize sein.

**Returns:**
int
### setByteCount(int value) {#setByteCount-int-}
```
public void setByteCount(int value)
```


Ruft die Byte‑Anzahl ab oder legt sie fest.

Wert: Ein 16‑Bit vorzeichenloser Integer, der die Größe der folgenden Datensatz‑Daten in Bytes angibt. Dieser Wert MUSS 34 plus den Wert des Feldes EnhancedMetafileDataSize sein.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | int |  |

### getData() {#getData--}
```
public byte[] getData()
```


Liest oder setzt die Daten.

Wert: Die Daten.

**Returns:**
byte[]
### setData(byte[] value) {#setData-byte---}
```
public void setData(byte[] value)
```


Liest oder setzt die Daten.

Wert: Die Daten.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | byte[] |  |

### getChecked() {#getChecked--}
```
public boolean getChecked()
```


Ruft einen Wert ab, der angibt, ob dieses `WmfEscapeRecordBase` aktiviert ist.

Wert: `true`, wenn aktiviert; andernfalls `false`.

**Returns:**
boolean
### setChecked(boolean value) {#setChecked-boolean-}
```
public void setChecked(boolean value)
```


Ruft einen Wert ab, der angibt, ob dieses `WmfEscapeRecordBase` aktiviert ist.

Wert: `true`, wenn aktiviert; andernfalls `false`.

**Parameters:**
| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| Wert | boolean |  |

