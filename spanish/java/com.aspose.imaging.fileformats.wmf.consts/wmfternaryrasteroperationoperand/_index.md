---
title: "WmfTernaryRasterOperationOperand"
second_title: "Referencia de la API de Aspose.Imaging para Java"
description: "Cada código de operación raster ternaria representa una operación booleana en la que los valores de los píxeles en la fuente, el pincel seleccionado y el destino se combinan."
type: docs
weight: 35
url: /es/java/com.aspose.imaging.fileformats.wmf.consts/wmfternaryrasteroperationoperand/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class WmfTernaryRasterOperationOperand extends System.Enum
```

Cada código de operación raster ternaria representa una operación booleana en la que los valores de los píxeles en la fuente, el pincel seleccionado y el destino se combinan. A continuación se presentan los tres operandos utilizados en estas operaciones.
## Campos

| Campo | Descripción |
| --- | --- |
| [D](#D) | Mapa de bits de destino |
| [P](#P) | Pincel seleccionado (también llamado patrón) |
| [S](#S) | Mapa de bits de origen |
| [a](#a) | AND a nivel de bits |
| [n](#n) | NOT a nivel de bits (inverso) |
| [o](#o) | OR a nivel de bits |
| [x](#x) | XOR a nivel de bits (OR exclusivo) |
### D {#D}
```
public static final int D
```


Mapa de bits de destino

### P {#P}
```
public static final int P
```


Pincel seleccionado (también llamado patrón)

### S {#S}
```
public static final int S
```


Mapa de bits de origen

### a {#a}
```
public static final int a
```


AND a nivel de bits

### n {#n}
```
public static final int n
```


NOT a nivel de bits (inverso)

### o {#o}
```
public static final int o
```


OR a nivel de bits

### x {#x}
```
public static final int x
```


XOR a nivel de bits (OR exclusivo)

