---
title: "TiffOptionsError"
second_title: "Referencia de la API de Aspose.Imaging para Java"
description: "Los códigos de error de las opciones TIFF."
type: docs
weight: 49
url: /es/java/com.aspose.imaging.imageoptions/tiffoptionserror/
---
**Inheritance:**
java.lang.Object, com.aspose.ms.System.ValueType, com.aspose.ms.System.Enum
```
public final class TiffOptionsError extends System.Enum
```

Los códigos de error de las opciones TIFF.
## Campos

| Campo | Descripción |
| --- | --- |
| [NoError](#NoError) | Sin código de error. |
| [NoColorMap](#NoColorMap) | El mapa de colores no está definido. |
| [ColorMapLengthInvalid](#ColorMapLengthInvalid) | La longitud del mapa de colores es inválida. |
| [CompressionSppMismatch](#CompressionSppMismatch) | La compresión no coincide con el recuento de muestras por píxel. |
| [PhotometricCompressionMismatch](#PhotometricCompressionMismatch) | La compresión no coincide con la configuración fotométrica. |
| [PhotometricSppMismatch](#PhotometricSppMismatch) | La configuración fotométrica no coincide con el recuento de muestras por píxel. |
| [NotSupportedAlphaStorage](#NotSupportedAlphaStorage) | El almacenamiento alfa no es compatible. |
| [PhotometricBitsPerSampleMismatch](#PhotometricBitsPerSampleMismatch) | Los bits fotométricos por muestra son inválidos |
| [Baseline6OptionsMismatch](#Baseline6OptionsMismatch) | Los parámetros especificados de opciones TIFF no se ajustan al estándar TIFF Baseline 6.0 |
### NoError {#NoError}
```
public static final int NoError
```


Sin código de error.

### NoColorMap {#NoColorMap}
```
public static final int NoColorMap
```


El mapa de colores no está definido.

### ColorMapLengthInvalid {#ColorMapLengthInvalid}
```
public static final int ColorMapLengthInvalid
```


La longitud del mapa de colores es inválida.

### CompressionSppMismatch {#CompressionSppMismatch}
```
public static final int CompressionSppMismatch
```


La compresión no coincide con el recuento de muestras por píxel.

### PhotometricCompressionMismatch {#PhotometricCompressionMismatch}
```
public static final int PhotometricCompressionMismatch
```


La compresión no coincide con la configuración fotométrica.

### PhotometricSppMismatch {#PhotometricSppMismatch}
```
public static final int PhotometricSppMismatch
```


La configuración fotométrica no coincide con el recuento de muestras por píxel.

### NotSupportedAlphaStorage {#NotSupportedAlphaStorage}
```
public static final int NotSupportedAlphaStorage
```


El almacenamiento alfa no es compatible.

### PhotometricBitsPerSampleMismatch {#PhotometricBitsPerSampleMismatch}
```
public static final int PhotometricBitsPerSampleMismatch
```


Los bits fotométricos por muestra son inválidos

### Baseline6OptionsMismatch {#Baseline6OptionsMismatch}
```
public static final int Baseline6OptionsMismatch
```


Los parámetros especificados de opciones TIFF no se ajustan al estándar TIFF Baseline 6.0

